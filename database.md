# 🗄️ 데이터베이스 (Database) - CS 문제은행

> 총 120문제 | Level 1~4

---

## 📖 주요 CS 용어 정리

### 데이터베이스 기초
- **DBMS(Database Management System)**: 데이터베이스를 관리하는 시스템 소프트웨어
- **RDBMS(Relational DBMS)**: 관계형 데이터베이스 관리 시스템 (MySQL, PostgreSQL, Oracle 등)
- **NoSQL**: 비관계형 데이터베이스 (MongoDB, Redis, Cassandra 등)
- **스키마(Schema)**: 데이터베이스의 구조와 제약조건을 정의
- **테이블(Table)**: 행과 열로 구성된 데이터 집합 (관계형 DB)
- **레코드(Record)**: 테이블의 한 행. 하나의 데이터 항목
- **필드(Field)**: 테이블의 열. 속성을 나타냄

### 키(Key)
- **기본키(Primary Key)**: 테이블에서 각 레코드를 고유하게 식별하는 키
- **외래키(Foreign Key)**: 다른 테이블의 기본키를 참조하는 키
- **후보키(Candidate Key)**: 기본키가 될 수 있는 속성들
- **슈퍼키(Super Key)**: 레코드를 유일하게 식별할 수 있는 속성 집합
- **복합키(Composite Key)**: 두 개 이상의 컬럼으로 구성된 키

### 트랜잭션
- **트랜잭션(Transaction)**: 데이터베이스 작업의 논리적 단위
- **ACID**: 트랜잭션의 4가지 특성
  - **Atomicity(원자성)**: 모두 성공 또는 모두 실패
  - **Consistency(일관성)**: 트랜잭션 전후 DB는 일관된 상태 유지
  - **Isolation(격리성)**: 동시 실행되는 트랜잭션들은 서로 영향 없음
  - **Durability(지속성)**: 완료된 트랜잭션 결과는 영구적으로 반영
- **커밋(Commit)**: 트랜잭션을 확정하여 DB에 반영
- **롤백(Rollback)**: 트랜잭션을 취소하고 이전 상태로 복구

### 정규화
- **정규화(Normalization)**: 데이터 중복을 최소화하고 무결성을 보장하는 설계 기법
- **1정규화(1NF)**: 원자값만 포함 (반복 그룹 제거)
- **2정규화(2NF)**: 부분 함수 종속 제거
- **3정규화(3NF)**: 이행 함수 종속 제거
- **BCNF**: 모든 결정자가 후보키
- **반정규화(Denormalization)**: 성능 향상을 위해 의도적으로 중복 허용

### 인덱스
- **인덱스(Index)**: 검색 속도를 높이기 위한 자료구조
- **B-Tree 인덱스**: 균형 트리 구조. RDBMS에서 가장 많이 사용
- **Hash 인덱스**: 해시 함수 기반. 등호(=) 검색에 빠름
- **클러스터 인덱스(Clustered Index)**: 데이터가 물리적으로 정렬됨
- **논클러스터 인덱스(Non-clustered Index)**: 별도의 인덱스 구조 유지

### SQL 명령어
- **DDL(Data Definition Language)**: 스키마 정의 (CREATE, ALTER, DROP)
- **DML(Data Manipulation Language)**: 데이터 조작 (SELECT, INSERT, UPDATE, DELETE)
- **DCL(Data Control Language)**: 권한 제어 (GRANT, REVOKE)
- **TCL(Transaction Control Language)**: 트랜잭션 제어 (COMMIT, ROLLBACK)
- **JOIN**: 두 개 이상의 테이블을 연결하여 조회
- **뷰(View)**: 하나 이상의 테이블로부터 유도된 가상 테이블
- **프로시저(Stored Procedure)**: DB에 저장된 SQL 프로그램

### 성능 & 확장
- **인덱스 스캔(Index Scan)**: 인덱스를 이용한 검색
- **풀 테이블 스캔(Full Table Scan)**: 테이블 전체를 순차적으로 검색
- **실행 계획(Execution Plan)**: 쿼리가 실행되는 방법을 보여주는 정보
- **샤딩(Sharding)**: 데이터를 여러 DB에 수평 분할
- **레플리케이션(Replication)**: 데이터를 여러 DB에 복제 (읽기 성능 향상)
- **파티셔닝(Partitioning)**: 큰 테이블을 작은 단위로 분할

### 동시성 제어
- **락(Lock)**: 동시 접근을 제어하는 메커니즘
- **공유 락(Shared Lock)**: 읽기 전용 락. 여러 트랜잭션이 동시에 획득 가능
- **배타 락(Exclusive Lock)**: 쓰기 락. 하나의 트랜잭션만 획득 가능
- **데드락(Deadlock)**: 두 개 이상의 트랜잭션이 서로의 락을 기다리며 무한 대기
- **격리 수준(Isolation Level)**: 트랜잭션 간 격리 정도 (READ UNCOMMITTED, READ COMMITTED, REPEATABLE READ, SERIALIZABLE)

### 🎯 시니어급 고급 용어

#### 트랜잭션 & 동시성 고급
- **MVCC(Multi-Version Concurrency Control)**: 읽기/쓰기 충돌을 방지하기 위해 여러 버전의 데이터를 유지
- **Snapshot Isolation**: 트랜잭션이 시작 시점의 스냅샷으로 일관된 읽기 수행
- **Optimistic Locking**: 충돌이 드물다고 가정하고 커밋 시점에 검증 (버전 번호 사용)
- **Pessimistic Locking**: 충돌 가능성이 높다고 가정하고 미리 락 획득
- **Intention Lock**: 계층적 락 구조에서 하위 레벨 락 의도를 표시 (IX, IS)
- **Gap Lock**: 범위 내 존재하지 않는 행에 대한 락 (Phantom Read 방지)
- **Next-Key Lock**: 레코드 락 + Gap 락 조합 (InnoDB 기본)
- **Two-Phase Locking(2PL)**: Growing Phase(락 획득만), Shrinking Phase(락 해제만) 두 단계로 구분
- **Phantom Read**: 같은 쿼리를 두 번 실행했을 때 새로운 행이 나타나는 현상
- **Dirty Read**: 커밋되지 않은 데이터를 읽는 현상
- **Non-Repeatable Read**: 같은 행을 두 번 읽었을 때 값이 달라지는 현상

#### 로그 & 복구
- **WAL(Write-Ahead Logging)**: 데이터 변경 전에 로그를 먼저 기록하여 장애 복구 보장
- **Redo Log**: 커밋된 트랜잭션을 재실행하여 복구
- **Undo Log**: 커밋되지 않은 트랜잭션을 취소하여 롤백
- **Checkpoint**: 메모리의 더티 페이지를 디스크에 기록하는 시점
- **PITR(Point-In-Time Recovery)**: 특정 시점으로 데이터베이스 복구
- **Binary Log(Binlog)**: 모든 변경 사항을 기록. 복제와 복구에 사용

#### 쿼리 최적화
- **Query Optimizer**: 쿼리의 최적 실행 계획을 선택하는 컴포넌트
- **Cost-Based Optimizer(CBO)**: 통계 정보를 기반으로 비용 계산하여 최적 계획 선택
- **Rule-Based Optimizer(RBO)**: 미리 정의된 규칙으로 실행 계획 선택
- **Cardinality**: 특정 컬럼의 고유 값 개수. 인덱스 선택성 판단에 사용
- **Selectivity**: 조건에 맞는 행의 비율. 낮을수록 인덱스가 효과적
- **Index Selectivity**: 인덱스로 걸러낼 수 있는 데이터의 비율
- **Covering Index**: 쿼리에 필요한 모든 컬럼을 포함하는 인덱스 (테이블 접근 불필요)
- **Index Skip Scan**: 복합 인덱스의 선행 컬럼을 건너뛰고 검색
- **Loose Index Scan**: 인덱스의 일부 값만 스캔 (GROUP BY 최적화)

#### 인덱스 고급
- **B+Tree**: B-Tree의 변형. 리프 노드에만 데이터 저장하고 연결 리스트로 연결
- **LSM-Tree(Log-Structured Merge-Tree)**: 쓰기 최적화 인덱스. 메모리에서 정렬 후 디스크에 병합
- **GiST(Generalized Search Tree)**: PostgreSQL의 확장 가능한 인덱스 구조
- **GIN(Generalized Inverted Index)**: 배열, JSON, 전문 검색용 인덱스
- **Bitmap Index**: 낮은 카디널리티 컬럼에 효과적 (성별, 상태 등)
- **Partial Index**: 조건을 만족하는 행만 인덱싱하여 크기 절감
- **Function-Based Index**: 함수 결과값에 대한 인덱스
- **Composite Index**: 여러 컬럼을 조합한 인덱스 (컬럼 순서 중요)

#### 분산 데이터베이스
- **CAP Theorem**: Consistency, Availability, Partition Tolerance 중 2개만 선택 가능
- **BASE**: Basically Available, Soft state, Eventually consistent (NoSQL 특성)
- **Eventual Consistency**: 일정 시간 후 모든 노드가 동일한 데이터를 가짐
- **Strong Consistency**: 모든 읽기가 최신 쓰기 결과를 반환
- **Quorum**: 분산 시스템에서 합의를 위한 최소 노드 수 (R + W > N)
- **Two-Phase Commit(2PC)**: 분산 트랜잭션 커밋 프로토콜 (Prepare → Commit)
- **Paxos**: 분산 합의 알고리즘. 노드 간 값 합의
- **Raft**: Paxos보다 이해하기 쉬운 합의 알고리즘 (Leader Election + Log Replication)
- **Vector Clock**: 분산 시스템에서 이벤트 순서를 추적하는 메커니즘
- **Consistent Hashing**: 노드 추가/제거 시 데이터 재분배를 최소화하는 해싱 기법

#### 레플리케이션 & 샤딩
- **Master-Slave Replication**: 마스터는 쓰기, 슬레이브는 읽기 담당
- **Master-Master Replication**: 여러 마스터가 쓰기 가능. 충돌 해결 필요
- **Synchronous Replication**: 모든 복제본에 쓰기 완료 후 커밋 (강한 일관성)
- **Asynchronous Replication**: 마스터 쓰기 완료 즉시 커밋 (복제 지연 가능)
- **Horizontal Sharding**: 행 단위로 데이터를 여러 DB에 분산
- **Vertical Sharding**: 컬럼 단위로 데이터를 분할
- **Hash-Based Sharding**: 해시 함수로 샤드 결정
- **Range-Based Sharding**: 범위로 샤드 결정 (1~1000, 1001~2000 등)
- **Resharding**: 샤드 재분배. 데이터 이동 필요

#### NoSQL 고급
- **Document Store**: JSON/BSON 형태로 저장 (MongoDB)
- **Key-Value Store**: 키-값 쌍으로 저장 (Redis, DynamoDB)
- **Column-Family Store**: 컬럼 단위로 저장 (Cassandra, HBase)
- **Graph Database**: 노드와 엣지로 관계 저장 (Neo4j)
- **Time-Series Database**: 시계열 데이터 최적화 (InfluxDB, TimescaleDB)
- **Memtable**: LSM-Tree에서 메모리 기반 쓰기 버퍼
- **SSTable(Sorted String Table)**: 디스크에 저장된 정렬된 키-값 파일
- **Compaction**: LSM-Tree에서 SSTable을 병합하여 공간 회수

#### 성능 & 모니터링
- **Connection Pool**: DB 연결을 미리 생성하여 재사용
- **Prepared Statement**: 쿼리를 미리 컴파일하여 재사용. SQL 인젝션 방지
- **Query Cache**: 쿼리 결과를 캐싱 (MySQL 5.7 이하)
- **Buffer Pool**: 자주 사용하는 데이터 페이지를 메모리에 캐싱
- **Dirty Page**: 메모리에서 수정되었지만 디스크에 기록되지 않은 페이지
- **Page Replacement**: 버퍼 풀이 가득 찰 때 페이지를 교체하는 알고리즘 (LRU)
- **Slow Query Log**: 임계값보다 오래 걸린 쿼리 기록
- **EXPLAIN**: 쿼리 실행 계획 분석 도구
- **Table Statistics**: 테이블 크기, 카디널리티 등 통계 정보. Optimizer가 사용

#### 뷰 & 고급 기능
- **Materialized View**: 쿼리 결과를 물리적으로 저장. 주기적으로 갱신
- **Trigger**: 특정 이벤트 발생 시 자동 실행되는 프로시저
- **Foreign Key Constraint**: 참조 무결성 보장. CASCADE, SET NULL 옵션
- **Check Constraint**: 컬럼 값의 유효성 검증
- **Unique Constraint**: 중복 값 방지
- **Sequence**: 자동 증가 값 생성 (Auto Increment)
- **Cursor**: 쿼리 결과를 한 행씩 처리하는 메커니즘
- **CTE(Common Table Expression)**: WITH 절로 임시 결과 집합 정의
- **Window Function**: 그룹화 없이 집계 함수 사용 (ROW_NUMBER, RANK, LAG, LEAD)

---

## Level 1 - 기초 (1~2년차) | 30문제

---

### Q1. 데이터베이스란?

#### A. 핵심 요약
구조화된 데이터를 저장, 관리, 검색하는 시스템입니다.

#### 상세 설명
- DBMS: 데이터베이스 관리 시스템
- 데이터 독립성, 일관성, 보안
- 동시 접근 제어

#### 실무 예시
- MySQL, PostgreSQL
- MongoDB, Redis

#### 면접 답변 예시
**1분**: "데이터베이스는 데이터를 체계적으로 저장하고 DBMS가 관리합니다."

**3분**: "파일 시스템과 달리 동시성 제어, 트랜잭션, 복구 기능을 제공합니다."

#### 꼬리 질문
- DBMS의 장점?
- 파일 시스템과 차이?

---

### Q2. 관계형 데이터베이스란?

#### A. 핵심 요약
테이블(릴레이션)로 데이터를 표현하고 SQL로 조작합니다.

#### 상세 설명
- 테이블, 행, 열
- 기본키, 외래키
- 정규화

#### 실무 예시
- MySQL, PostgreSQL, Oracle
- 비즈니스 데이터

#### 면접 답변 예시
**1분**: "관계형 DB는 테이블로 데이터를 표현하고 관계로 연결합니다."

**3분**: "ACID 특성으로 데이터 일관성을 보장합니다. SQL로 복잡한 쿼리가 가능합니다."

#### 꼬리 질문
- NoSQL과 차이?
- 관계형의 한계?

---

### Q3. 기본키(Primary Key)란?

#### A. 핵심 요약
테이블에서 각 행을 고유하게 식별하는 속성입니다.

#### 상세 설명
- 유일성, NOT NULL
- 자연키 vs 대리키
- 복합 기본키

#### 실무 예시
- 사용자 ID
- 주문 번호

#### 면접 답변 예시
**1분**: "기본키는 행을 고유하게 식별하며, NULL이 될 수 없습니다."

**3분**: "대리키(AUTO_INCREMENT)가 관리 편하고, 자연키는 의미가 있지만 변경 가능성이 있습니다."

#### 꼬리 질문
- 자연키 vs 대리키?
- UUID vs Auto Increment?

---

### Q4. 외래키(Foreign Key)란?

#### A. 핵심 요약
다른 테이블의 기본키를 참조하여 관계를 형성합니다.

#### 상세 설명
- 참조 무결성
- CASCADE, SET NULL
- 관계 유형 (1:1, 1:N, M:N)

#### 실무 예시
- 주문-고객 관계
- 게시글-댓글

#### 면접 답변 예시
**1분**: "외래키는 다른 테이블을 참조하여 관계를 정의하고 무결성을 보장합니다."

**3분**: "ON DELETE CASCADE로 부모 삭제 시 자식도 삭제합니다. 성능 영향을 고려해야 합니다."

#### 꼬리 질문
- CASCADE 옵션들?
- FK 없이 관계 표현?

---

### Q5. SQL 기본 명령어는?

#### A. 핵심 요약
SELECT, INSERT, UPDATE, DELETE로 데이터를 조작합니다.

#### 상세 설명
- SELECT: 조회
- INSERT: 삽입
- UPDATE: 수정
- DELETE: 삭제

#### 실무 예시
- CRUD 작업
- 데이터 분석

#### 면접 답변 예시
**1분**: "SELECT로 조회, INSERT/UPDATE/DELETE로 데이터를 변경합니다."

**3분**: "WHERE 절로 조건 지정, ORDER BY로 정렬, LIMIT로 결과 제한합니다."

#### 꼬리 질문
- TRUNCATE vs DELETE?
- UPDATE와 락?

---

### Q6. JOIN이란?

#### A. 핵심 요약
여러 테이블의 데이터를 결합하여 조회합니다.

#### 상세 설명
- INNER JOIN: 교집합
- LEFT/RIGHT JOIN: 한쪽 전체 + 매칭
- CROSS JOIN: 카테시안 곱

#### 실무 예시
- 주문과 고객 정보 함께 조회
- 여러 테이블 연계

#### 면접 답변 예시
**1분**: "JOIN은 테이블을 연결하여 관련 데이터를 함께 조회합니다."

**3분**: "INNER는 양쪽 일치만, LEFT는 왼쪽 전체와 일치하는 오른쪽을 반환합니다."

#### 꼬리 질문
- LEFT vs INNER 차이?
- JOIN 성능 최적화?

---

### Q7. 인덱스란?

#### A. 핵심 요약
검색 속도를 높이기 위한 자료구조입니다.

#### 상세 설명
- B+Tree 구조
- 검색 O(log n)
- 쓰기 오버헤드

#### 실무 예시
- 자주 검색하는 컬럼
- WHERE, JOIN 조건

#### 면접 답변 예시
**1분**: "인덱스는 B+Tree로 검색을 O(log n)으로 가속합니다."

**3분**: "삽입/수정 시 인덱스 유지 비용이 있습니다. 선택도가 높은 컬럼에 효과적입니다."

#### 꼬리 질문
- 인덱스를 안 쓰는 경우?
- 복합 인덱스 순서?

---

### Q8. 트랜잭션이란?

#### A. 핵심 요약
하나의 논리적 작업 단위로, 모두 성공하거나 모두 실패합니다.

#### 상세 설명
- COMMIT, ROLLBACK
- ACID 특성
- 동시성 제어

#### 실무 예시
- 계좌 이체
- 주문 처리

#### 면접 답변 예시
**1분**: "트랜잭션은 원자적 작업 단위로, 중간 상태가 없습니다."

**3분**: "ACID로 데이터 일관성을 보장합니다. 분산 환경에서는 2PC가 필요합니다."

#### 꼬리 질문
- ACID 각각 설명?
- 분산 트랜잭션?

---

### Q9. ACID란?

#### A. 핵심 요약
트랜잭션의 4가지 특성: 원자성, 일관성, 격리성, 지속성입니다.

#### 상세 설명
- Atomicity: 전부 또는 전무
- Consistency: 규칙 유지
- Isolation: 동시 실행 격리
- Durability: 영구 저장

#### 실무 예시
- 금융 거래
- 재고 관리

#### 면접 답변 예시
**1분**: "ACID는 트랜잭션의 신뢰성을 보장하는 4가지 특성입니다."

**3분**: "격리성은 레벨에 따라 트레이드오프가 있습니다. 지속성은 WAL로 보장합니다."

#### 꼬리 질문
- BASE와 차이?
- CAP 정리?

---

### Q10. 정규화란?

#### A. 핵심 요약
데이터 중복을 줄이고 무결성을 높이는 테이블 설계 과정입니다.

#### 상세 설명
- 1NF: 원자값
- 2NF: 부분 종속 제거
- 3NF: 이행 종속 제거

#### 실무 예시
- 테이블 설계
- 이상 현상 방지

#### 면접 답변 예시
**1분**: "정규화는 중복을 제거하여 삽입/수정/삭제 이상을 방지합니다."

**3분**: "보통 3NF까지 적용하고, 성능을 위해 역정규화하기도 합니다."

#### 꼬리 질문
- 이상 현상 종류?
- 역정규화란?

---

### Q11. GROUP BY와 집계 함수는?

#### A. 핵심 요약
데이터를 그룹화하고 SUM, COUNT, AVG 등으로 집계합니다.

#### 상세 설명
- COUNT, SUM, AVG, MAX, MIN
- HAVING: 그룹 필터
- GROUP BY 컬럼

#### 실무 예시
- 일별 매출 합계
- 카테고리별 상품 수

#### 면접 답변 예시
**1분**: "GROUP BY로 그룹화하고 집계 함수로 요약 통계를 구합니다."

**3분**: "WHERE는 그룹화 전, HAVING은 그룹화 후 필터링입니다."

#### 꼬리 질문
- WHERE vs HAVING?
- ROLLUP, CUBE?

---

### Q12. 서브쿼리란?

#### A. 핵심 요약
쿼리 안에 포함된 쿼리로, 중첩 검색을 합니다.

#### 상세 설명
- 스칼라: 단일 값
- 인라인 뷰: FROM 절
- 상관: 외부 참조

#### 실무 예시
- 조건에 다른 쿼리 결과 사용
- 복잡한 조건

#### 면접 답변 예시
**1분**: "서브쿼리는 다른 쿼리 내에서 결과를 제공합니다."

**3분**: "상관 서브쿼리는 행마다 실행되어 느릴 수 있습니다. JOIN으로 대체하면 성능이 좋습니다."

#### 꼬리 질문
- 상관 vs 비상관?
- EXISTS vs IN?

---

### Q13. 뷰(View)란?

#### A. 핵심 요약
저장된 쿼리로, 가상 테이블처럼 사용합니다.

#### 상세 설명
- 데이터 저장 안 함
- 보안, 편의성
- Materialized View

#### 실무 예시
- 복잡한 쿼리 캡슐화
- 권한 제어

#### 면접 답변 예시
**1분**: "뷰는 쿼리에 이름을 붙인 것으로, 테이블처럼 조회합니다."

**3분**: "Materialized View는 결과를 저장하여 성능을 높입니다."

#### 꼬리 질문
- View vs Materialized View?
- 뷰 업데이트 가능 조건?

---

### Q14. 저장 프로시저란?

#### A. 핵심 요약
데이터베이스에 저장된 프로그램으로, SQL을 묶어 실행합니다.

#### 상세 설명
- 재사용성
- 네트워크 왕복 감소
- 디버깅 어려움

#### 실무 예시
- 복잡한 비즈니스 로직
- 배치 처리

#### 면접 답변 예시
**1분**: "저장 프로시저는 DB에 저장된 SQL 프로그램으로, 호출하여 실행합니다."

**3분**: "여러 SQL을 한 번에 실행하여 네트워크 왕복을 줄입니다. 하지만 버전 관리가 어렵습니다."

#### 꼬리 질문
- 장단점?
- 함수와 차이?

---

### Q15. 트리거란?

#### A. 핵심 요약
특정 이벤트 발생 시 자동 실행되는 프로시저입니다.

#### 상세 설명
- BEFORE/AFTER
- INSERT/UPDATE/DELETE
- 행 레벨, 문 레벨

#### 실무 예시
- 감사 로그
- 파생 데이터 갱신

#### 면접 답변 예시
**1분**: "트리거는 데이터 변경 시 자동으로 실행되는 프로시저입니다."

**3분**: "감사, 유효성 검사에 유용하지만 디버깅이 어렵고 성능 영향이 있습니다."

#### 꼬리 질문
- 트리거 남용 문제?
- 대안은?

---

### Q16. NULL 처리는?

#### A. 핵심 요약
값이 없음을 나타내며, 특별한 처리가 필요합니다.

#### 상세 설명
- NULL = NULL은 알 수 없음
- IS NULL, COALESCE
- 3값 논리

#### 실무 예시
- 선택적 필드
- 집계 시 제외

#### 면접 답변 예시
**1분**: "NULL은 값이 없음으로, 비교 시 IS NULL을 사용합니다."

**3분**: "COALESCE로 기본값을 지정하고, 집계 함수는 NULL을 무시합니다."

#### 꼬리 질문
- NULL과 빈 문자열?
- COUNT(*)과 COUNT(col)?

---

### Q17. DISTINCT란?

#### A. 핵심 요약
중복 행을 제거하고 유일한 값만 반환합니다.

#### 상세 설명
- SELECT DISTINCT
- 전체 행 기준
- 정렬 오버헤드

#### 실무 예시
- 유일한 값 목록
- 중복 제거

#### 면접 답변 예시
**1분**: "DISTINCT는 결과에서 중복 행을 제거합니다."

**3분**: "내부적으로 정렬이나 해싱이 필요하여 성능에 영향이 있습니다."

#### 꼬리 질문
- GROUP BY와 차이?
- 성능 고려?

---

### Q18. UNION과 UNION ALL은?

#### A. 핵심 요약
여러 쿼리 결과를 합칩니다. UNION은 중복 제거, ALL은 전부 포함입니다.

#### 상세 설명
- 컬럼 수, 타입 일치 필요
- UNION: 정렬+중복제거
- UNION ALL: 단순 결합

#### 실무 예시
- 여러 테이블 통합
- 보고서

#### 면접 답변 예시
**1분**: "UNION은 중복을 제거하고, UNION ALL은 모두 포함하여 빠릅니다."

**3분**: "중복이 없다면 UNION ALL이 성능이 좋습니다."

#### 꼬리 질문
- INTERSECT, EXCEPT?
- 성능 차이?

---

### Q19. ORDER BY와 LIMIT은?

#### A. 핵심 요약
ORDER BY로 정렬하고 LIMIT으로 결과 수를 제한합니다.

#### 상세 설명
- ASC, DESC
- OFFSET으로 페이지네이션
- 정렬 비용

#### 실무 예시
- 최신순 조회
- 페이지네이션

#### 면접 답변 예시
**1분**: "ORDER BY로 정렬하고 LIMIT으로 상위 N개를 가져옵니다."

**3분**: "OFFSET이 크면 성능 저하됩니다. 커서 기반 페이지네이션이 대안입니다."

#### 꼬리 질문
- OFFSET 페이지네이션 문제?
- 커서 페이지네이션?

---

### Q20. 데이터 타입 선택은?

#### A. 핵심 요약
저장 공간, 성능, 정확성을 고려하여 적절한 타입을 선택합니다.

#### 상세 설명
- INT vs BIGINT
- VARCHAR vs TEXT
- DECIMAL vs FLOAT

#### 실무 예시
- 금액: DECIMAL
- ID: INT/BIGINT

#### 면접 답변 예시
**1분**: "데이터 범위에 맞는 최소 크기 타입을 선택합니다."

**3분**: "금액은 DECIMAL, 과학 계산은 FLOAT, 긴 문자열은 TEXT를 사용합니다."

#### 꼬리 질문
- CHAR vs VARCHAR?
- JSON 타입 사용?

---

### Q21. 제약조건(Constraint)이란?

#### A. 핵심 요약
데이터 무결성을 보장하기 위해 컬럼에 적용하는 규칙입니다.

#### 상세 설명
- NOT NULL: 빈 값 불가
- UNIQUE: 중복 불가
- CHECK: 조건 검사
- DEFAULT: 기본값

#### 실무 예시
- 이메일 형식 검사
- 나이 범위 제한

#### 면접 답변 예시
**1분**: "제약조건은 데이터베이스 레벨에서 데이터 무결성을 강제합니다."

**3분**: "애플리케이션 검증과 별도로 DB 제약조건을 두면 이중 보안이 됩니다."

#### 꼬리 질문
- 애플리케이션 검증 vs DB 제약조건?
- CHECK 제약조건 성능?

---

### Q22. 엔티티와 관계란?

#### A. 핵심 요약
엔티티는 실세계 개체, 관계는 엔티티 간 연결입니다.

#### 상세 설명
- 엔티티: 테이블로 표현
- 관계: 1:1, 1:N, M:N
- ERD로 시각화

#### 실무 예시
- 고객-주문 관계
- 학생-수강-과목 M:N

#### 면접 답변 예시
**1분**: "엔티티는 테이블, 관계는 외래키로 표현됩니다."

**3분**: "M:N 관계는 중간 테이블(조인 테이블)로 분해하여 구현합니다."

#### 꼬리 질문
- M:N 관계 구현?
- ERD 도구?

---

### Q23. LIKE 검색이란?

#### A. 핵심 요약
패턴 매칭으로 문자열을 검색합니다.

#### 상세 설명
- %: 0개 이상 문자
- _: 정확히 1개 문자
- 인덱스 사용 제한

#### 실무 예시
- 이름 검색
- 키워드 필터

#### 면접 답변 예시
**1분**: "LIKE는 와일드카드로 패턴 매칭 검색을 합니다."

**3분**: "'%keyword'는 인덱스를 못 타지만, 'keyword%'는 인덱스를 탈 수 있습니다."

#### 꼬리 질문
- Full-text search와 차이?
- 인덱스 사용 조건?

---

### Q24. CASE 문이란?

#### A. 핵심 요약
조건에 따라 다른 값을 반환하는 SQL 조건문입니다.

#### 상세 설명
- CASE WHEN ... THEN ... ELSE ... END
- 중첩 가능
- SELECT, WHERE, ORDER BY에서 사용

#### 실무 예시
- 등급 분류
- 조건별 계산

#### 면접 답변 예시
**1분**: "CASE문은 SQL에서 if-else 역할을 합니다."

**3분**: "집계와 함께 사용하면 피벗 테이블처럼 결과를 변환할 수 있습니다."

#### 꼬리 질문
- COALESCE, NULLIF와 차이?
- 피벗 테이블 만들기?

---

### Q25. AUTO_INCREMENT란?

#### A. 핵심 요약
삽입 시 자동으로 증가하는 값을 생성합니다.

#### 상세 설명
- 시퀀스, Identity
- 고유 ID 생성
- 갭 발생 가능

#### 실무 예시
- 기본키 ID
- 주문 번호

#### 면접 답변 예시
**1분**: "AUTO_INCREMENT는 삽입 시 자동으로 고유 번호를 부여합니다."

**3분**: "롤백 시 번호가 건너뛰어질 수 있습니다. 분산 환경에서는 UUID가 나을 수 있습니다."

#### 꼬리 질문
- 갭이 생기는 이유?
- 분산 환경에서의 대안?

---

### Q26. DATE와 TIMESTAMP 차이는?

#### A. 핵심 요약
DATE는 날짜만, TIMESTAMP는 날짜와 시간 + 타임존을 저장합니다.

#### 상세 설명
- DATE: 2024-01-01
- TIMESTAMP: 타임존 인식
- DATETIME: 타임존 미인식

#### 실무 예시
- 생년월일: DATE
- 주문 시각: TIMESTAMP

#### 면접 답변 예시
**1분**: "TIMESTAMP는 타임존을 인식하여 글로벌 서비스에 적합합니다."

**3분**: "서버 타임존이 바뀌어도 TIMESTAMP는 자동 변환됩니다."

#### 꼬리 질문
- UTC 저장 전략?
- 2038년 문제?

---

### Q27. 인라인 뷰란?

#### A. 핵심 요약
FROM 절에 사용되는 서브쿼리입니다.

#### 상세 설명
- 임시 테이블처럼 사용
- 별칭 필수
- 복잡한 쿼리 분해

#### 실무 예시
- 집계 후 필터
- 순위 계산

#### 면접 답변 예시
**1분**: "인라인 뷰는 FROM 절의 서브쿼리로, 결과를 테이블처럼 사용합니다."

**3분**: "CTE(WITH 절)가 가독성이 더 좋아 최근에는 CTE를 선호합니다."

#### 꼬리 질문
- CTE와 차이?
- 성능 영향?

---

### Q28. EXISTS와 IN 차이는?

#### A. 핵심 요약
EXISTS는 존재 여부, IN은 값 일치 여부를 확인합니다.

#### 상세 설명
- EXISTS: 상관 서브쿼리, 첫 매칭 시 종료
- IN: 목록 전체 비교
- NULL 처리 차이

#### 실무 예시
- 특정 조건 존재 확인
- 필터링

#### 면접 답변 예시
**1분**: "EXISTS는 존재하면 즉시 종료, IN은 전체를 비교합니다."

**3분**: "서브쿼리 결과가 크면 EXISTS가 효율적이고, NOT IN은 NULL에 주의해야 합니다."

#### 꼬리 질문
- NOT IN의 NULL 문제?
- 언제 어떤 것을 사용?

---

### Q29. 집합 연산자란?

#### A. 핵심 요약
두 쿼리 결과를 집합 연산으로 결합합니다.

#### 상세 설명
- UNION: 합집합
- INTERSECT: 교집합
- EXCEPT/MINUS: 차집합

#### 실무 예시
- 통합 보고서
- 데이터 비교

#### 면접 답변 예시
**1분**: "집합 연산자는 두 SELECT 결과를 합치거나 비교합니다."

**3분**: "컬럼 수와 타입이 일치해야 합니다. INTERSECT는 양쪽 모두 있는 행만 반환합니다."

#### 꼬리 질문
- EXCEPT 활용 예시?
- 성능 고려사항?

---

### Q30. 스키마란?

#### A. 핵심 요약
데이터베이스의 논리적 구조와 구성을 정의합니다.

#### 상세 설명
- 테이블, 뷰, 인덱스 정의
- 네임스페이스 역할
- DDL로 관리

#### 실무 예시
- 스키마별 권한 분리
- 멀티테넌트 구조

#### 면접 답변 예시
**1분**: "스키마는 데이터베이스 객체를 논리적으로 그룹화합니다."

**3분**: "PostgreSQL에서는 스키마로 테넌트를 분리하는 멀티테넌트 설계가 가능합니다."

#### 꼬리 질문
- 카탈로그, 스키마, 테이블 관계?
- 스키마 버전 관리?

---

## Level 2 - 중급 (3~4년차) | 36문제

---

### Q31. 실행 계획이란?

#### A. 핵심 요약
쿼리를 어떻게 실행할지 보여주는 옵티마이저의 계획입니다.

#### 상세 설명
- EXPLAIN / EXPLAIN ANALYZE
- 테이블 스캔 vs 인덱스 스캔
- 조인 방식, 비용 추정

#### 실무 예시
- 쿼리 튜닝
- 인덱스 효과 확인

#### 면접 답변 예시
**1분**: "실행 계획은 옵티마이저가 선택한 쿼리 실행 방법을 보여줍니다."

**3분**: "Full Scan이면 인덱스가 없거나 선택도가 낮습니다. 조인 순서와 방식도 확인합니다."

#### 꼬리 질문
- 비용 기반 옵티마이저?
- 힌트 사용?

---

### Q32. 인덱스 종류는?

#### A. 핵심 요약
B-Tree, Hash, Full-text, 공간 인덱스 등이 있습니다.

#### 상세 설명
- B-Tree: 범위 검색
- Hash: 등호 검색
- 복합, 커버링, 부분

#### 실무 예시
- 검색 패턴에 맞는 인덱스
- 복합 인덱스 설계

#### 면접 답변 예시
**1분**: "B-Tree가 가장 일반적이고 범위 검색을 지원합니다."

**3분**: "커버링 인덱스는 테이블 접근 없이 인덱스만으로 쿼리를 처리합니다."

#### 꼬리 질문
- 커버링 인덱스란?
- 함수 기반 인덱스?

---

### Q33. 트랜잭션 격리 수준은?

#### A. 핵심 요약
동시 트랜잭션 간 간섭 정도를 결정합니다.

#### 상세 설명
- Read Uncommitted, Read Committed
- Repeatable Read, Serializable
- 격리 수준별 문제

#### 실무 예시
- 기본값 확인
- 요구사항에 맞는 수준 선택

#### 면접 답변 예시
**1분**: "격리 수준이 높을수록 일관성은 좋지만 동시성은 낮습니다."

**3분**: "MySQL은 Repeatable Read, PostgreSQL은 Read Committed가 기본입니다."

#### 꼬리 질문
- Dirty Read, Phantom Read?
- MVCC란?

---

### Q34. 락(Lock)이란?

#### A. 핵심 요약
동시 접근을 제어하여 데이터 일관성을 유지합니다.

#### 상세 설명
- 공유 락(S), 배타 락(X)
- 행 락, 테이블 락
- 데드락

#### 실무 예시
- SELECT FOR UPDATE
- 락 경합 모니터링

#### 면접 답변 예시
**1분**: "락은 동시 수정을 막아 일관성을 유지합니다."

**3분**: "행 락이 테이블 락보다 동시성이 좋습니다. 데드락은 타임아웃으로 해결합니다."

#### 꼬리 질문
- 데드락 해결?
- 낙관적 vs 비관적 락?

---

### Q35. MVCC란?

#### A. 핵심 요약
다중 버전으로 읽기와 쓰기를 동시에 허용합니다.

#### 상세 설명
- 읽기는 락 없이
- 스냅샷 격리
- 버전 관리 오버헤드

#### 실무 예시
- PostgreSQL, MySQL InnoDB
- 높은 동시성

#### 면접 답변 예시
**1분**: "MVCC는 이전 버전을 유지하여 읽기 시 락 없이 일관된 뷰를 제공합니다."

**3분**: "PostgreSQL은 튜플 버전, MySQL은 언두 로그를 사용합니다."

#### 꼬리 질문
- Vacuum이란?
- 긴 트랜잭션 영향?

---

### Q36. 파티셔닝이란?

#### A. 핵심 요약
큰 테이블을 작은 단위로 분할하여 관리합니다.

#### 상세 설명
- Range, List, Hash
- 파티션 프루닝
- 유지보수 용이

#### 실무 예시
- 로그 테이블 날짜별
- 대용량 데이터

#### 면접 답변 예시
**1분**: "파티셔닝은 테이블을 분할하여 쿼리 성능과 관리를 개선합니다."

**3분**: "파티션 키를 잘 선택해야 프루닝이 효과적입니다. 파티션별 삭제가 빠릅니다."

#### 꼬리 질문
- 샤딩과 차이?
- 파티션 키 선택?

---

### Q37. 레플리케이션이란?

#### A. 핵심 요약
데이터를 여러 서버에 복제하여 가용성과 읽기 성능을 높입니다.

#### 상세 설명
- 마스터-슬레이브
- 동기 vs 비동기
- 복제 지연

#### 실무 예시
- 읽기 분산
- 장애 복구

#### 면접 답변 예시
**1분**: "레플리케이션은 데이터를 복제하여 읽기 확장과 가용성을 제공합니다."

**3분**: "비동기는 빠르지만 지연이 있고, 동기는 일관성이 좋지만 느립니다."

#### 꼬리 질문
- 복제 지연 문제?
- 페일오버?

---

### Q38. 커넥션 풀이란?

#### A. 핵심 요약
데이터베이스 연결을 재사용하여 오버헤드를 줄입니다.

#### 상세 설명
- 연결 생성 비용 절약
- 최대/최소 크기
- 타임아웃 설정

#### 실무 예시
- HikariCP, PgBouncer
- 서버 설정

#### 면접 답변 예시
**1분**: "커넥션 풀은 연결을 미리 만들어 재사용합니다."

**3분**: "풀 크기가 너무 크면 메모리 낭비, 작으면 대기가 발생합니다."

#### 꼬리 질문
- 적정 풀 크기?
- 커넥션 누수?

---

### Q39. 쿼리 최적화 기법은?

#### A. 핵심 요약
인덱스, 쿼리 재작성, 실행 계획 분석으로 성능을 개선합니다.

#### 상세 설명
- 인덱스 활용
- 서브쿼리 → JOIN
- N+1 문제

#### 실무 예시
- 슬로우 쿼리 분석
- 인덱스 추가

#### 면접 답변 예시
**1분**: "실행 계획을 분석하고 적절한 인덱스와 쿼리 구조를 선택합니다."

**3분**: "불필요한 컬럼 제거, 조건 범위 축소, JOIN 순서 조정 등을 고려합니다."

#### 꼬리 질문
- N+1 문제 해결?
- 쿼리 힌트?

---

### Q40. 벌크 작업이란?

#### A. 핵심 요약
대량 데이터를 효율적으로 삽입/수정/삭제합니다.

#### 상세 설명
- Batch INSERT
- COPY / LOAD DATA
- 인덱스 비활성화

#### 실무 예시
- 데이터 마이그레이션
- ETL

#### 면접 답변 예시
**1분**: "벌크 작업은 행 단위 대신 일괄 처리하여 성능을 높입니다."

**3분**: "인덱스를 끄고 작업 후 다시 만들면 더 빠릅니다."

#### 꼬리 질문
- 트랜잭션 처리?
- 진행 상황 추적?

---

### Q41. 조인 알고리즘은?

#### A. 핵심 요약
Nested Loop, Hash Join, Merge Join 등이 있습니다.

#### 상세 설명
- Nested Loop: 작은 테이블에 적합
- Hash Join: 등호 조인, 메모리 사용
- Merge Join: 정렬된 데이터, 범위 조인

#### 실무 예시
- 실행 계획에서 조인 방식 확인
- 힌트로 조인 방식 지정

#### 면접 답변 예시
**1분**: "조인 알고리즘은 테이블 크기와 조건에 따라 옵티마이저가 선택합니다."

**3분**: "작은 테이블은 Nested Loop, 큰 테이블의 등호 조인은 Hash Join이 효율적입니다."

#### 꼬리 질문
- 조인 순서 중요성?
- Hash Join 메모리 부족 시?

---

### Q42. 복합 인덱스 설계 원칙은?

#### A. 핵심 요약
선택도 높은 컬럼을 앞에, 범위 조건은 뒤에 배치합니다.

#### 상세 설명
- 최좌측 접두사 원칙
- 등호 조건 먼저
- 커버링 인덱스 고려

#### 실무 예시
- (status, created_at) 인덱스
- (user_id, product_id, created_at)

#### 면접 답변 예시
**1분**: "복합 인덱스는 컬럼 순서가 중요하며, 왼쪽부터 매칭됩니다."

**3분**: "WHERE status = 'active' AND created_at > '2024-01-01'이면 status가 앞에 와야 합니다."

#### 꼬리 질문
- 인덱스 스킵 스캔?
- 인덱스 컬럼 순서 결정 기준?

---

### Q43. 데드락 분석과 해결은?

#### A. 핵심 요약
교착 상태를 모니터링하고 트랜잭션 설계로 예방합니다.

#### 상세 설명
- 데드락 로그 분석
- 락 획득 순서 통일
- 타임아웃 설정

#### 실무 예시
- SHOW ENGINE INNODB STATUS
- pg_locks 조회

#### 면접 답변 예시
**1분**: "데드락은 락 순서를 통일하고 트랜잭션을 짧게 유지하여 예방합니다."

**3분**: "데드락 발생 시 한 트랜잭션이 롤백됩니다. 재시도 로직을 구현해야 합니다."

#### 꼬리 질문
- 데드락 그래프?
- 재시도 전략?

---

### Q44. 슬로우 쿼리 분석은?

#### A. 핵심 요약
느린 쿼리를 찾아 실행 계획을 분석하고 개선합니다.

#### 상세 설명
- slow_query_log 설정
- EXPLAIN ANALYZE
- pt-query-digest

#### 실무 예시
- 주기적 슬로우 쿼리 리뷰
- APM 연동

#### 면접 답변 예시
**1분**: "슬로우 쿼리 로그를 활성화하고 정기적으로 분석합니다."

**3분**: "실행 계획에서 Full Table Scan, filesort, temporary를 확인하고 인덱스를 추가합니다."

#### 꼬리 질문
- filesort란?
- 쿼리 프로파일링?

---

### Q45. 읽기/쓰기 분리란?

#### A. 핵심 요약
쓰기는 마스터, 읽기는 슬레이브로 분산합니다.

#### 상세 설명
- 복제 지연 고려
- 세션 일관성
- 프록시 사용

#### 실무 예시
- ProxySQL, HAProxy
- Spring @Transactional(readOnly)

#### 면접 답변 예시
**1분**: "읽기/쓰기 분리로 마스터 부하를 줄이고 읽기 확장성을 높입니다."

**3분**: "쓰기 직후 읽기는 마스터에서 하거나 지연을 허용하는 설계가 필요합니다."

#### 꼬리 질문
- 복제 지연 대응?
- Sticky session?

---

### Q46. 캐시 전략(Cache Strategy)은?

#### A. 핵심 요약
Cache-Aside, Write-Through, Write-Behind 등이 있습니다.

#### 상세 설명
- Cache-Aside: 애플리케이션 관리
- Write-Through: 동시 기록
- Write-Behind: 비동기 기록

#### 실무 예시
- Redis + MySQL 조합
- 캐시 무효화 전략

#### 면접 답변 예시
**1분**: "Cache-Aside가 가장 일반적이며, 캐시 미스 시 DB에서 로드합니다."

**3분**: "TTL과 이벤트 기반 무효화를 조합하여 일관성을 관리합니다."

#### 꼬리 질문
- 캐시 웜업?
- 캐시 스탬피드?

---

### Q47. ORM 사용 시 주의점은?

#### A. 핵심 요약
N+1 문제, 지연 로딩, 쿼리 효율성에 주의해야 합니다.

#### 상세 설명
- N+1 문제
- Eager vs Lazy Loading
- 네이티브 쿼리 필요 시점

#### 실무 예시
- JPA fetch join
- QueryDSL, jOOQ

#### 면접 답변 예시
**1분**: "ORM은 생산성이 좋지만 N+1 문제와 비효율적 쿼리에 주의해야 합니다."

**3분**: "fetch join으로 N+1을 해결하고, 복잡한 쿼리는 네이티브 SQL을 사용합니다."

#### 꼬리 질문
- N+1 문제 해결 방법?
- Batch size 설정?

---

### Q48. 낙관적 락 vs 비관적 락?

#### A. 핵심 요약
낙관적 락은 버전 체크, 비관적 락은 실제 락을 걸어 충돌을 방지합니다.

#### 상세 설명
- 낙관적: version 컬럼, UPDATE 시 체크
- 비관적: SELECT FOR UPDATE
- 충돌 빈도에 따라 선택

#### 실무 예시
- 낙관적: 게시글 수정
- 비관적: 좌석 예매

#### 면접 답변 예시
**1분**: "충돌이 드물면 낙관적 락, 자주 발생하면 비관적 락을 사용합니다."

**3분**: "낙관적 락은 성능이 좋지만 충돌 시 재시도가 필요합니다."

#### 꼬리 질문
- 동시성 높은 시스템에서의 선택?
- 락 에스컬레이션?

---

### Q49. 데이터 모델링 3단계는?

#### A. 핵심 요약
개념적, 논리적, 물리적 모델링 순서로 진행합니다.

#### 상세 설명
- 개념: 엔티티, 관계 정의
- 논리: 정규화, 속성 정의
- 물리: 인덱스, 파티션, 타입

#### 실무 예시
- ERD 도구 사용
- 리뷰 프로세스

#### 면접 답변 예시
**1분**: "개념 → 논리 → 물리 순서로 점점 구체화됩니다."

**3분**: "개념 모델링에서 비즈니스 요구사항을 파악하고, 물리 모델링에서 성능을 고려합니다."

#### 꼬리 질문
- 정규화 수준 결정?
- 역정규화 시점?

---

### Q50. 서로게이트 키 vs 자연 키?

#### A. 핵심 요약
서로게이트 키는 의미 없는 ID, 자연 키는 비즈니스 의미가 있는 키입니다.

#### 상세 설명
- 서로게이트: auto_increment, UUID
- 자연: 주민번호, 이메일
- 외래키, 조인 성능

#### 실무 예시
- 대부분 서로게이트 키 사용
- 복합 자연 키 피하기

#### 면접 답변 예시
**1분**: "서로게이트 키가 변경에 강하고 조인 성능이 좋습니다."

**3분**: "자연 키는 변경 가능성이 있어 서로게이트 키를 기본으로 하고, 자연 키는 UNIQUE로 관리합니다."

#### 꼬리 질문
- UUID 성능 문제?
- 복합 키의 단점?

---

### Q51. 감사 로그 설계는?

#### A. 핵심 요약
데이터 변경 이력을 추적하기 위한 테이블 설계입니다.

#### 상세 설명
- created_at, updated_at, deleted_at
- 변경 전/후 값 저장
- 별도 이력 테이블

#### 실무 예시
- 감사 추적
- 데이터 복구

#### 면접 답변 예시
**1분**: "모든 변경을 추적할 수 있도록 이력 테이블을 설계합니다."

**3분**: "트리거나 CDC(Change Data Capture)로 자동 기록하고, 보관 기간을 정합니다."

#### 꼬리 질문
- Soft Delete 전략?
- CDC 도구?

---

### Q52. 시퀀스와 Identity 차이는?

#### A. 핵심 요약
시퀀스는 독립 객체, Identity는 테이블에 종속된 자동 증가입니다.

#### 상세 설명
- 시퀀스: 여러 테이블 공유 가능
- Identity: 테이블별 독립
- 캐싱, 갭

#### 실무 예시
- Oracle: 시퀀스
- MySQL: AUTO_INCREMENT

#### 면접 답변 예시
**1분**: "시퀀스는 테이블과 독립적이어서 유연하게 사용할 수 있습니다."

**3분**: "시퀀스는 미리 캐싱하여 성능이 좋지만, 롤백 시 번호가 낭비될 수 있습니다."

#### 꼬리 질문
- 시퀀스 캐시 설정?
- 분산 환경 ID 생성?

---

### Q53. 윈도우 함수란?

#### A. 핵심 요약
행 그룹에 대해 집계하면서 개별 행을 유지합니다.

#### 상세 설명
- ROW_NUMBER, RANK, DENSE_RANK
- SUM() OVER, AVG() OVER
- PARTITION BY, ORDER BY

#### 실무 예시
- 순위 계산
- 누적 합계

#### 면접 답변 예시
**1분**: "윈도우 함수는 GROUP BY 없이 그룹별 계산을 합니다."

**3분**: "ROW_NUMBER()로 그룹별 순위를 매기고, 페이지네이션이나 중복 제거에 활용합니다."

#### 꼬리 질문
- RANK vs DENSE_RANK?
- LAG, LEAD 함수?

---

### Q54. CTE(Common Table Expression)란?

#### A. 핵심 요약
WITH 절로 임시 결과 집합을 정의하여 가독성을 높입니다.

#### 상세 설명
- 재귀 CTE 가능
- 쿼리 구조화
- 뷰와 유사

#### 실무 예시
- 복잡한 쿼리 분해
- 계층 구조 조회

#### 면접 답변 예시
**1분**: "CTE는 WITH 절로 서브쿼리에 이름을 붙여 가독성을 높입니다."

**3분**: "재귀 CTE로 조직도, 카테고리 트리 같은 계층 구조를 쿼리할 수 있습니다."

#### 꼬리 질문
- 재귀 CTE 종료 조건?
- 성능 영향?

---

### Q55. Upsert(MERGE)란?

#### A. 핵심 요약
있으면 UPDATE, 없으면 INSERT 하는 작업입니다.

#### 상세 설명
- INSERT ON DUPLICATE KEY UPDATE
- MERGE 문
- ON CONFLICT (PostgreSQL)

#### 실무 예시
- 설정 저장
- 동기화

#### 면접 답변 예시
**1분**: "Upsert는 존재 여부에 따라 삽입/수정을 한 번에 처리합니다."

**3분**: "레이스 컨디션을 방지하고, 별도 SELECT 없이 원자적으로 처리합니다."

#### 꼬리 질문
- 동시성 문제?
- 벌크 Upsert?

---

### Q56. 데이터 암호화 전략은?

#### A. 핵심 요약
저장(at rest), 전송(in transit), 애플리케이션 레벨로 구분됩니다.

#### 상세 설명
- TDE: 투명한 데이터 암호화
- SSL/TLS: 전송 암호화
- 컬럼 암호화

#### 실무 예시
- 개인정보 암호화
- 결제 데이터

#### 면접 답변 예시
**1분**: "저장 데이터는 TDE, 전송은 SSL, 민감한 컬럼은 애플리케이션에서 암호화합니다."

**3분**: "키 관리가 중요합니다. KMS를 사용하고 키 로테이션을 설정합니다."

#### 꼬리 질문
- 암호화된 컬럼 검색?
- 키 관리 전략?

---

### Q57. 백업 전략은?

#### A. 핵심 요약
풀 백업, 증분 백업, WAL 아카이빙을 조합합니다.

#### 상세 설명
- 풀 백업: 전체 스냅샷
- 증분: 변경분만
- PITR: 시점 복구

#### 실무 예시
- 일간 풀 + 시간별 증분
- 3-2-1 백업 규칙

#### 면접 답변 예시
**1분**: "풀 백업과 증분 백업을 조합하여 RPO를 최소화합니다."

**3분**: "WAL 아카이빙으로 특정 시점까지 복구할 수 있습니다. 정기 복구 테스트가 필수입니다."

#### 꼬리 질문
- RPO vs RTO?
- 백업 검증?

---

### Q58. 연결 타임아웃 설정은?

#### A. 핵심 요약
연결, 쿼리, 유휴 타임아웃을 적절히 설정합니다.

#### 상세 설명
- connect_timeout
- query_timeout / statement_timeout
- idle_in_transaction_session_timeout

#### 실무 예시
- 커넥션 풀 설정
- 슬로우 쿼리 강제 종료

#### 면접 답변 예시
**1분**: "타임아웃으로 장애 전파를 막고 리소스 낭비를 방지합니다."

**3분**: "유휴 트랜잭션이 오래 열려있으면 락 문제가 생깁니다. idle_timeout을 설정해야 합니다."

#### 꼬리 질문
- 적정 타임아웃 값?
- 타임아웃 모니터링?

---

### Q59. JSON 데이터 타입 사용은?

#### A. 핵심 요약
스키마리스 데이터를 관계형 DB에 저장할 수 있습니다.

#### 상세 설명
- JSON vs JSONB
- JSON 인덱스 (GIN)
- 쿼리 연산자

#### 실무 예시
- 설정, 메타데이터 저장
- 유연한 속성

#### 면접 답변 예시
**1분**: "JSON 타입으로 유연한 구조를 저장하고 쿼리할 수 있습니다."

**3분**: "PostgreSQL JSONB는 바이너리로 저장되어 인덱싱과 쿼리가 효율적입니다."

#### 꼬리 질문
- JSON vs 정규화?
- GIN 인덱스?

---

### Q60. 쿼리 힌트란?

#### A. 핵심 요약
옵티마이저에게 실행 계획을 지시하는 방법입니다.

#### 상세 설명
- 인덱스 힌트
- 조인 순서 힌트
- 남용 주의

#### 실무 예시
- FORCE INDEX
- USE HASH JOIN

#### 면접 답변 예시
**1분**: "힌트는 옵티마이저가 잘못된 계획을 선택할 때 강제로 지정합니다."

**3분**: "힌트에 의존하면 데이터 분포 변화 시 문제가 됩니다. 근본 원인을 해결해야 합니다."

#### 꼬리 질문
- 힌트 남용 문제?
- 통계 갱신이 우선?

---

### Q61. 임시 테이블 vs CTE vs 서브쿼리?

#### A. 핵심 요약
용도와 성능에 따라 적절한 방식을 선택합니다.

#### 상세 설명
- 임시 테이블: 인덱스 가능, 여러 번 참조
- CTE: 가독성, 일회성
- 서브쿼리: 간단한 경우

#### 실무 예시
- 대용량 중간 결과: 임시 테이블
- 복잡한 쿼리 분해: CTE

#### 면접 답변 예시
**1분**: "여러 번 참조하면 임시 테이블, 가독성이 중요하면 CTE를 사용합니다."

**3분**: "임시 테이블에 인덱스를 만들어 성능을 높일 수 있습니다."

#### 꼬리 질문
- 메모리 임시 테이블?
- 구체화된 CTE?

---

### Q62. Batch 처리와 커서 사용은?

#### A. 핵심 요약
대량 데이터는 배치로 나누어 처리하고, 커서는 행 단위 처리에 사용합니다.

#### 상세 설명
- 청크 단위 처리
- 서버 사이드 커서
- 메모리 관리

#### 실무 예시
- 대량 업데이트
- 리포트 생성

#### 면접 답변 예시
**1분**: "대량 데이터는 배치로 나누어 락과 메모리 문제를 방지합니다."

**3분**: "LIMIT/OFFSET이나 커서로 청크 단위 처리합니다. 트랜잭션 범위도 고려해야 합니다."

#### 꼬리 질문
- 배치 크기 결정?
- 커서의 단점?

---

### Q63. 풀 텍스트 검색이란?

#### A. 핵심 요약
자연어 검색을 위한 특수 인덱스와 검색 기능입니다.

#### 상세 설명
- 역인덱스
- 형태소 분석
- 순위 매기기

#### 실무 예시
- 게시글 검색
- 상품 검색

#### 면접 답변 예시
**1분**: "풀 텍스트 검색은 LIKE보다 빠르고 자연어 검색이 가능합니다."

**3분**: "Elasticsearch가 더 강력하지만, DB 내장 FTS도 간단한 경우 유용합니다."

#### 꼬리 질문
- Elasticsearch와 비교?
- 한글 형태소 분석?

---

### Q64. 실행 계획 캐싱이란?

#### A. 핵심 요약
파싱된 실행 계획을 재사용하여 오버헤드를 줄입니다.

#### 상세 설명
- Prepared Statement
- 플랜 캐시
- 파라미터 바인딩

#### 실무 예시
- 반복 쿼리 최적화
- SQL Injection 방지

#### 면접 답변 예시
**1분**: "Prepared Statement는 실행 계획을 캐싱하고 SQL Injection을 방지합니다."

**3분**: "파라미터 값에 따라 다른 계획이 최적일 수 있어, 일부 DB는 적응형 계획을 사용합니다."

#### 꼬리 질문
- 파라미터 스니핑?
- 동적 SQL과 성능?

---

### Q65. 통계 정보 관리는?

#### A. 핵심 요약
정확한 통계가 좋은 실행 계획의 기반입니다.

#### 상세 설명
- ANALYZE / UPDATE STATISTICS
- 히스토그램
- 자동 vs 수동

#### 실무 예시
- 대량 로드 후 통계 갱신
- 주기적 유지보수

#### 면접 답변 예시
**1분**: "옵티마이저는 통계 정보로 카디널리티를 추정합니다. 정기적으로 갱신해야 합니다."

**3분**: "데이터 분포가 급변하면 통계가 오래되어 잘못된 계획을 선택합니다."

#### 꼬리 질문
- 히스토그램이란?
- 자동 통계 수집 설정?

---

### Q66. 파라미터 스니핑이란?

#### A. 핵심 요약
첫 실행 시 파라미터로 계획이 고정되어 다른 값에서 비효율적일 수 있습니다.

#### 상세 설명
- 바인드 픽
- 적응형 커서
- OPTIMIZE FOR 힌트

#### 실무 예시
- 범위가 넓은 파라미터
- 데이터 분포 편향

#### 면접 답변 예시
**1분**: "첫 파라미터로 최적화된 계획이 다른 값에는 부적합할 수 있습니다."

**3분**: "극단적인 값이 먼저 실행되면 문제가 됩니다. OPTION(RECOMPILE)이나 힌트로 대응합니다."

#### 꼬리 질문
- 해결 방법?
- 적응형 쿼리 처리?

---

## Level 3 - 고급 (5~7년차) | 30문제

---

### Q67. B+Tree 구조와 동작은?

#### A. 핵심 요약
모든 데이터가 리프에 있고, 리프가 연결되어 범위 검색이 효율적입니다.

#### 상세 설명
- 내부 노드: 키만
- 리프 노드: 키 + 데이터
- 분할, 병합

#### 실무 예시
- InnoDB 인덱스
- PostgreSQL 인덱스

#### 면접 답변 예시
**1분**: "B+Tree는 리프에만 데이터가 있어 범위 스캔이 효율적입니다."

**3분**: "페이지 크기에 맞춰 노드를 구성하여 디스크 I/O를 최소화합니다."

#### 꼬리 질문
- B-Tree와 차이?
- 페이지 분할 비용?

---

### Q68. 옵티마이저 동작 원리는?

#### A. 핵심 요약
비용 기반으로 최적의 실행 계획을 선택합니다.

#### 상세 설명
- 통계 정보
- 비용 모델
- 조인 순서, 접근 방법

#### 실무 예시
- ANALYZE로 통계 갱신
- 힌트로 조정

#### 면접 답변 예시
**1분**: "옵티마이저는 통계를 기반으로 비용이 낮은 계획을 선택합니다."

**3분**: "통계가 오래되면 잘못된 계획을 선택할 수 있습니다. 정기적 ANALYZE가 필요합니다."

#### 꼬리 질문
- 카디널리티 추정?
- 힌트 사용 시기?

---

### Q69. WAL(Write-Ahead Logging)이란?

#### A. 핵심 요약
변경 전 로그를 먼저 기록하여 복구를 보장합니다.

#### 상세 설명
- Redo 로그
- 체크포인트
- 크래시 복구

#### 실무 예시
- 트랜잭션 지속성
- 포인트-인-타임 복구

#### 면접 답변 예시
**1분**: "WAL은 데이터 변경 전 로그를 기록하여 크래시 복구를 보장합니다."

**3분**: "체크포인트로 복구 시간을 줄이고, 아카이빙으로 시점 복구를 합니다."

#### 꼬리 질문
- 체크포인트란?
- 풀 페이지 기록?

---

### Q70. 분산 데이터베이스란?

#### A. 핵심 요약
데이터를 여러 노드에 분산하여 확장성을 확보합니다.

#### 상세 설명
- 샤딩
- 분산 트랜잭션
- CAP 정리

#### 실무 예시
- CockroachDB, Vitess
- 대규모 서비스

#### 면접 답변 예시
**1분**: "분산 DB는 데이터를 노드에 분산하여 단일 서버 한계를 극복합니다."

**3분**: "CAP에서 P를 선택하면 C와 A 중 트레이드오프가 있습니다."

#### 꼬리 질문
- 2PC 문제점?
- Spanner 동작?

---

### Q71. 샤딩 전략은?

#### A. 핵심 요약
데이터를 키 기준으로 여러 데이터베이스에 분할합니다.

#### 상세 설명
- 범위, 해시, 디렉토리
- 핫스팟 방지
- 리샤딩

#### 실무 예시
- 사용자 ID 기반
- 테넌트 기반

#### 면접 답변 예시
**1분**: "샤딩은 샤드 키로 데이터를 분산하여 수평 확장합니다."

**3분**: "Consistent Hashing으로 리샤딩 영향을 줄입니다. 크로스 샤드 쿼리는 피해야 합니다."

#### 꼬리 질문
- 샤드 키 선택 기준?
- 크로스 샤드 트랜잭션?

---

### Q72. NewSQL이란?

#### A. 핵심 요약
분산 환경에서 SQL과 ACID를 제공하는 새로운 데이터베이스입니다.

#### 상세 설명
- 분산 트랜잭션
- 수평 확장
- Raft/Paxos 합의

#### 실무 예시
- CockroachDB, TiDB
- 클라우드 네이티브

#### 면접 답변 예시
**1분**: "NewSQL은 분산 확장성과 ACID를 모두 제공합니다."

**3분**: "Spanner의 TrueTime, CockroachDB의 Hybrid Logical Clock으로 분산 트랜잭션을 처리합니다."

#### 꼬리 질문
- 기존 RDBMS와 차이?
- 합의 알고리즘 역할?

---

### Q73. LSM Tree란?

#### A. 핵심 요약
쓰기에 최적화된 자료구조로, 메모리에 먼저 쓰고 배경에서 병합합니다.

#### 상세 설명
- MemTable + SSTable
- Compaction
- Bloom Filter

#### 실무 예시
- RocksDB, LevelDB
- Cassandra, HBase

#### 면접 답변 예시
**1분**: "LSM Tree는 쓰기가 빠르고, 읽기는 Bloom Filter로 최적화합니다."

**3분**: "쓰기는 순차적이라 빠르고, Compaction으로 읽기 성능을 유지합니다."

#### 꼬리 질문
- B+Tree와 비교?
- Write Amplification?

---

### Q74. 인덱스 온리 스캔이란?

#### A. 핵심 요약
테이블 접근 없이 인덱스만으로 쿼리를 처리합니다.

#### 상세 설명
- 커버링 인덱스
- Visibility Map
- 조건과 SELECT 컬럼 모두 인덱스에 포함

#### 실무 예시
- 집계 쿼리 최적화
- COUNT(*) 최적화

#### 면접 답변 예시
**1분**: "인덱스만으로 결과를 반환하여 테이블 랜덤 I/O를 제거합니다."

**3분**: "PostgreSQL은 Visibility Map을 사용하여 인덱스 온리 스캔을 지원합니다."

#### 꼬리 질문
- MVCC와 인덱스 온리 스캔?
- 커버링 인덱스 설계?

---

### Q75. 갭 락(Gap Lock)이란?

#### A. 핵심 요약
인덱스 레코드 사이의 간격을 잠가 Phantom Read를 방지합니다.

#### 상세 설명
- InnoDB Repeatable Read
- 범위 조건에서 발생
- Next-Key Lock

#### 실무 예시
- 범위 검색 시 락
- 데드락 원인

#### 면접 답변 예시
**1분**: "갭 락은 레코드 사이 공간을 잠가 새로운 행 삽입을 막습니다."

**3분**: "Phantom Read를 방지하지만, 동시성이 떨어지고 데드락 원인이 될 수 있습니다."

#### 꼬리 질문
- Next-Key Lock이란?
- 갭 락 비활성화?

---

### Q76. Vacuum이란?

#### A. 핵심 요약
PostgreSQL에서 죽은 튜플을 정리하고 통계를 갱신합니다.

#### 상세 설명
- Dead Tuple 정리
- 트랜잭션 ID 랩어라운드 방지
- AutoVacuum

#### 실무 예시
- 테이블 팽창 방지
- 성능 유지

#### 면접 답변 예시
**1분**: "Vacuum은 MVCC로 생긴 죽은 행을 정리합니다."

**3분**: "AutoVacuum 설정을 테이블 특성에 맞게 조정해야 합니다. 대용량 테이블은 공격적으로 설정합니다."

#### 꼬리 질문
- Vacuum Full이란?
- 트랜잭션 ID 랩어라운드?

---

### Q77. Connection Pool Sizing은?

#### A. 핵심 요약
CPU 코어, 디스크 I/O, 네트워크를 고려하여 적정 크기를 설정합니다.

#### 상세 설명
- 공식: 코어 * 2 + 디스크
- 너무 크면 경합
- 모니터링 기반 조정

#### 실무 예시
- HikariCP 설정
- PgBouncer 튜닝

#### 면접 답변 예시
**1분**: "풀이 너무 크면 컨텍스트 스위칭, 너무 작으면 대기가 발생합니다."

**3분**: "connections = ((core_count * 2) + effective_spindle_count) 공식을 시작점으로 합니다."

#### 꼬리 질문
- 풀 크기 모니터링 지표?
- 멀티 데이터소스 환경?

---

### Q78. Query Plan Regression이란?

#### A. 핵심 요약
통계 변화나 업그레이드로 실행 계획이 나빠지는 현상입니다.

#### 상세 설명
- 통계 변화
- 버전 업그레이드
- Plan Baseline

#### 실무 예시
- 갑작스런 쿼리 성능 저하
- 배포 후 문제

#### 면접 답변 예시
**1분**: "실행 계획이 갑자기 바뀌어 성능이 저하되는 현상입니다."

**3분**: "SQL Plan Baseline으로 검증된 계획을 고정하고, 새 계획은 검증 후 적용합니다."

#### 꼬리 질문
- Plan Baseline 관리?
- A/B 테스트?

---

### Q79. Point-in-Time Recovery란?

#### A. 핵심 요약
특정 시점으로 데이터베이스를 복구합니다.

#### 상세 설명
- 베이스 백업 + WAL
- 복구 목표 시점 지정
- 재해 복구

#### 실무 예시
- 실수로 데이터 삭제
- 랜섬웨어 대응

#### 면접 답변 예시
**1분**: "PITR은 풀 백업과 WAL을 조합하여 원하는 시점으로 복구합니다."

**3분**: "WAL 아카이빙을 S3 등에 저장하고, recovery_target_time으로 복구 시점을 지정합니다."

#### 꼬리 질문
- 복구 시간 단축 방법?
- 증분 백업 vs WAL?

---

### Q80. 데이터베이스 프록시란?

#### A. 핵심 요약
클라이언트와 DB 사이에서 연결 관리, 라우팅, 보안을 담당합니다.

#### 상세 설명
- 커넥션 풀링
- 읽기/쓰기 분리
- 쿼리 라우팅

#### 실무 예시
- ProxySQL, PgBouncer
- AWS RDS Proxy

#### 면접 답변 예시
**1분**: "프록시는 연결 풀링으로 DB 부하를 줄이고 자동 라우팅을 제공합니다."

**3분**: "서버리스 환경에서 연결 폭증을 방지하고, 페일오버를 투명하게 처리합니다."

#### 꼬리 질문
- 프록시 오버헤드?
- 트랜잭션 모드 vs 세션 모드?

---

### Q81. 이벤트 소싱과 CQRS란?

#### A. 핵심 요약
상태 변경을 이벤트로 저장하고, 읽기와 쓰기를 분리합니다.

#### 상세 설명
- 이벤트 저장소
- 프로젝션
- 최종 일관성

#### 실무 예시
- 금융 거래 이력
- 감사 추적

#### 면접 답변 예시
**1분**: "이벤트 소싱은 모든 변경을 이벤트로 저장하여 완전한 이력을 유지합니다."

**3분**: "CQRS로 쓰기는 이벤트 저장소, 읽기는 최적화된 뷰를 사용합니다."

#### 꼬리 질문
- 이벤트 버전 관리?
- Snapshot 전략?

---

### Q82. 분산 락 구현은?

#### A. 핵심 요약
여러 노드에서 동시 접근을 제어하는 락입니다.

#### 상세 설명
- Redis Redlock
- ZooKeeper
- 펜싱 토큰

#### 실무 예시
- 중복 결제 방지
- 리더 선출

#### 면접 답변 예시
**1분**: "분산 환경에서 Redis나 ZooKeeper로 상호 배제를 구현합니다."

**3분**: "Redlock은 과반수 노드에서 락을 획득합니다. 펜싱 토큰으로 좀비 프로세스를 방지합니다."

#### 꼬리 질문
- Redlock 논란?
- 락 갱신 전략?

---

### Q83. Change Data Capture란?

#### A. 핵심 요약
데이터 변경을 실시간으로 캡처하여 다른 시스템에 전파합니다.

#### 상세 설명
- 로그 기반 CDC
- Debezium
- Kafka Connect

#### 실무 예시
- 캐시 동기화
- 검색 인덱스 갱신

#### 면접 답변 예시
**1분**: "CDC는 DB 변경 로그를 읽어 실시간으로 다른 시스템에 전파합니다."

**3분**: "Debezium + Kafka로 마이크로서비스 간 데이터 동기화를 구현합니다."

#### 꼬리 질문
- 폴링 vs 로그 기반?
- 스키마 진화?

---

### Q84. Raft/Paxos 합의 알고리즘이란?

#### A. 핵심 요약
분산 시스템에서 노드 간 합의를 이루는 알고리즘입니다.

#### 상세 설명
- 리더 선출
- 로그 복제
- 과반수 합의

#### 실무 예시
- etcd, CockroachDB
- 분산 DB 복제

#### 면접 답변 예시
**1분**: "Raft는 리더가 로그를 복제하고 과반수 확인 후 커밋합니다."

**3분**: "Paxos보다 이해하기 쉽고 구현이 간단합니다. 네트워크 파티션 시 과반수 쪽이 진행됩니다."

#### 꼬리 질문
- 리더 선출 과정?
- 네트워크 파티션 대응?

---

### Q85. 멀티 테넌시 데이터 격리는?

#### A. 핵심 요약
테넌트별 데이터를 분리하는 전략입니다.

#### 상세 설명
- 테넌트 컬럼
- 스키마별 분리
- DB별 분리

#### 실무 예시
- SaaS 애플리케이션
- 보안 요구사항

#### 면접 답변 예시
**1분**: "보안, 성능, 비용에 따라 컬럼, 스키마, DB 분리를 선택합니다."

**3분**: "컬럼 분리는 효율적이지만 격리가 약하고, DB 분리는 격리가 강하지만 비용이 높습니다."

#### 꼬리 질문
- Row-Level Security?
- 테넌트 마이그레이션?

---

### Q86. 글로벌 데이터베이스 설계는?

#### A. 핵심 요약
지역별 지연 시간과 데이터 일관성을 고려한 설계입니다.

#### 상세 설명
- 지역별 복제
- 충돌 해결
- Active-Active

#### 실무 예시
- 글로벌 서비스
- 지역별 규정 준수

#### 면접 답변 예시
**1분**: "지역별 복제로 지연을 줄이고, 충돌 해결 전략을 정의합니다."

**3분**: "Last-Write-Wins, CRDT, 애플리케이션 병합 등 충돌 해결 방법을 선택합니다."

#### 꼬리 질문
- CRDT란?
- GDPR과 데이터 위치?

---

### Q87. 데이터베이스 모니터링 지표는?

#### A. 핵심 요약
QPS, 지연 시간, 연결 수, 복제 지연 등을 모니터링합니다.

#### 상세 설명
- 쿼리 성능
- 리소스 사용률
- 락과 대기

#### 실무 예시
- Prometheus + Grafana
- DataDog, New Relic

#### 면접 답변 예시
**1분**: "QPS, 지연 시간, 활성 연결, 복제 지연을 핵심 지표로 모니터링합니다."

**3분**: "이상 탐지와 알림을 설정하고, 대시보드로 트렌드를 파악합니다."

#### 꼬리 질문
- 주요 알림 설정?
- 용량 계획?

---

### Q88. Online DDL이란?

#### A. 핵심 요약
서비스 중단 없이 스키마를 변경합니다.

#### 상세 설명
- ALGORITHM=INPLACE
- pt-online-schema-change
- gh-ost

#### 실무 예시
- 대용량 테이블 인덱스 추가
- 컬럼 타입 변경

#### 면접 답변 예시
**1분**: "Online DDL로 락을 최소화하며 스키마를 변경합니다."

**3분**: "gh-ost는 binlog를 읽어 섀도우 테이블에 변경을 적용하고 테이블을 교체합니다."

#### 꼬리 질문
- gh-ost 동작 원리?
- 롤백 방법?

---

### Q89. 쿼리 타임아웃과 취소는?

#### A. 핵심 요약
장시간 쿼리를 타임아웃으로 제한하고 취소합니다.

#### 상세 설명
- statement_timeout
- 쿼리 취소 메커니즘
- 리소스 보호

#### 실무 예시
- API 타임아웃 연동
- 배치 쿼리 제한

#### 면접 답변 예시
**1분**: "statement_timeout으로 쿼리 실행 시간을 제한합니다."

**3분**: "취소 시 이미 사용한 리소스는 정리되어야 합니다. 트랜잭션 롤백이 필요할 수 있습니다."

#### 꼬리 질문
- 취소 불가능한 작업?
- 타임아웃 전파?

---

### Q90. 비동기 복제 vs 동기 복제?

#### A. 핵심 요약
비동기는 빠르지만 데이터 손실 가능, 동기는 안전하지만 느립니다.

#### 상세 설명
- 비동기: 성능 우선
- 동기: 일관성 우선
- 준동기 (Semi-sync)

#### 실무 예시
- 금융: 동기/준동기
- 일반: 비동기

#### 면접 답변 예시
**1분**: "동기 복제는 슬레이브 확인 후 커밋하여 데이터 손실을 방지합니다."

**3분**: "MySQL Semi-sync는 적어도 하나의 슬레이브 확인 후 커밋합니다."

#### 꼬리 질문
- 동기 복제의 지연?
- 페일오버 시 데이터 손실?

---

### Q91. 카디널리티와 선택도란?

#### A. 핵심 요약
카디널리티는 고유 값 수, 선택도는 조건 매칭 비율입니다.

#### 상세 설명
- 높은 카디널리티 → 인덱스 효과적
- 선택도 = 매칭 행 / 전체 행
- 옵티마이저 결정 요소

#### 실무 예시
- 인덱스 컬럼 선택
- 실행 계획 분석

#### 면접 답변 예시
**1분**: "카디널리티가 높은 컬럼에 인덱스가 효과적입니다."

**3분**: "선택도가 낮으면 (많은 행 매칭) 인덱스보다 풀 스캔이 나을 수 있습니다."

#### 꼬리 질문
- 선택도 추정 오류?
- 복합 인덱스에서 카디널리티?

---

### Q92. 페이지 분할과 단편화란?

#### A. 핵심 요약
데이터 삽입으로 페이지가 분할되고 단편화가 발생합니다.

#### 상세 설명
- B+Tree 페이지 분할
- 단편화로 성능 저하
- REINDEX, OPTIMIZE

#### 실무 예시
- 랜덤 삽입 패턴
- 정기 유지보수

#### 면접 답변 예시
**1분**: "페이지 분할은 삽입으로 페이지가 쪼개지면서 I/O가 증가합니다."

**3분**: "UUID PK는 랜덤 삽입으로 분할이 자주 발생합니다. 순차 ID가 성능에 유리합니다."

#### 꼬리 질문
- 단편화 측정?
- OPTIMIZE TABLE?

---

### Q93. 호스트 기반 인증이란?

#### A. 핵심 요약
IP 주소 기반으로 데이터베이스 접근을 제어합니다.

#### 상세 설명
- pg_hba.conf
- 화이트리스트
- VPN, VPC

#### 실무 예시
- 내부 네트워크만 허용
- 서비스별 접근 제어

#### 면접 답변 예시
**1분**: "IP 기반으로 접근을 제한하여 보안을 강화합니다."

**3분**: "VPC 내부에서만 접근을 허용하고, 외부는 VPN이나 SSL 터널을 사용합니다."

#### 꼬리 질문
- 인증 방식 종류?
- 역할 기반 접근 제어?

---

### Q94. 쿼리 리라이트란?

#### A. 핵심 요약
옵티마이저가 쿼리를 더 효율적인 형태로 변환합니다.

#### 상세 설명
- 서브쿼리 → 조인
- 조건 푸시다운
- 중복 조건 제거

#### 실무 예시
- 실행 계획 변환
- 자동 최적화

#### 면접 답변 예시
**1분**: "옵티마이저가 비효율적인 쿼리를 더 나은 형태로 변환합니다."

**3분**: "IN 서브쿼리를 세미조인으로, 상관 서브쿼리를 조인으로 변환합니다."

#### 꼬리 질문
- 리라이트 규칙?
- 변환 안 되는 경우?

---

### Q95. 시계열 데이터베이스란?

#### A. 핵심 요약
시간 기반 데이터를 효율적으로 저장하고 조회합니다.

#### 상세 설명
- 시간 기반 파티셔닝
- 압축
- 집계 쿼리 최적화

#### 실무 예시
- 메트릭 저장
- IoT 센서 데이터

#### 면접 답변 예시
**1분**: "시계열 DB는 타임스탬프 기반 쓰기와 범위 조회에 최적화되어 있습니다."

**3분**: "TimescaleDB는 PostgreSQL 확장으로, InfluxDB는 전용 시계열 DB입니다."

#### 꼬리 질문
- 관계형 DB와 비교?
- 다운샘플링?

---

### Q96. 그래프 데이터베이스란?

#### A. 핵심 요약
노드와 엣지로 관계를 표현하고 탐색하는 데이터베이스입니다.

#### 상세 설명
- 노드, 엣지, 속성
- 그래프 순회
- Cypher 쿼리

#### 실무 예시
- 소셜 네트워크
- 추천 시스템

#### 면접 답변 예시
**1분**: "그래프 DB는 관계 탐색에 특화되어 다단계 조인이 효율적입니다."

**3분**: "관계형에서 재귀 조인이 필요한 쿼리가 그래프 DB에서는 간단해집니다."

#### 꼬리 질문
- Neo4j 사용 사례?
- 관계형 vs 그래프 DB?

---

## Level 4 - 실무 & 아키텍처 (8~10년차) | 24문제

---

### Q97. 대규모 데이터베이스 설계는?

#### A. 핵심 요약
샤딩, 파티셔닝, 캐싱을 조합하여 PB급 데이터를 관리합니다.

#### 상세 설명
- 읽기/쓰기 분리
- 계층적 스토리지
- 콜드/핫 데이터

#### 실무 예시
- 글로벌 서비스
- 빅데이터 플랫폼

#### 면접 답변 예시
**1분**: "샤딩으로 쓰기, 레플리카로 읽기를 확장하고, 캐시로 부하를 줄입니다."

**3분**: "데이터 수명 주기에 따라 핫 데이터는 SSD, 콜드 데이터는 S3 등 계층화합니다."

#### 꼬리 질문
- 데이터 수명 주기 관리?
- 멀티 리전 설계?

---

### Q98. 데이터베이스 마이그레이션 전략은?

#### A. 핵심 요약
무중단 마이그레이션으로 스키마와 데이터를 안전하게 이동합니다.

#### 상세 설명
- 온라인 스키마 변경
- 이중 쓰기
- 점진적 전환

#### 실무 예시
- 버전 업그레이드
- 클라우드 이전

#### 면접 답변 예시
**1분**: "이중 쓰기로 데이터 동기화 후 점진적으로 전환합니다."

**3분**: "gh-ost, pt-online-schema-change로 락 없이 스키마를 변경합니다."

#### 꼬리 질문
- 롤백 전략?
- 데이터 검증?

---

### Q99. 데이터 파이프라인 설계는?

#### A. 핵심 요약
수집, 처리, 저장, 분석 단계를 연결하는 데이터 흐름입니다.

#### 상세 설명
- ETL vs ELT
- 실시간 vs 배치
- 데이터 품질

#### 실무 예시
- Airflow, Spark
- Kafka Streams

#### 면접 답변 예시
**1분**: "데이터 파이프라인은 데이터를 수집, 변환, 적재하는 자동화된 흐름입니다."

**3분**: "실시간은 Kafka + Flink, 배치는 Airflow + Spark 조합이 일반적입니다."

#### 꼬리 질문
- 데이터 품질 검증?
- 파이프라인 모니터링?

---

### Q100. 데이터 레이크 vs 데이터 웨어하우스?

#### A. 핵심 요약
레이크는 원시 데이터 저장, 웨어하우스는 구조화된 분석용 저장소입니다.

#### 상세 설명
- 데이터 레이크: 스키마 온 리드
- 웨어하우스: 스키마 온 라이트
- 레이크하우스

#### 실무 예시
- S3 + Athena
- Snowflake, BigQuery

#### 면접 답변 예시
**1분**: "레이크는 유연하고 저렴하며, 웨어하우스는 빠른 분석에 최적화되어 있습니다."

**3분**: "레이크하우스는 둘의 장점을 결합하여 Delta Lake, Iceberg 형식을 사용합니다."

#### 꼬리 질문
- 데이터 거버넌스?
- Delta Lake란?

---

### Q101. 데이터베이스 버전 업그레이드 전략은?

#### A. 핵심 요약
무중단 업그레이드를 위한 계획과 롤백 방안을 준비합니다.

#### 상세 설명
- 호환성 검증
- 블루-그린 업그레이드
- 롤링 업그레이드

#### 실무 예시
- PostgreSQL 메이저 업그레이드
- MySQL 업그레이드

#### 면접 답변 예시
**1분**: "호환성 테스트 후 복제본 업그레이드 → 페일오버 방식으로 진행합니다."

**3분**: "쿼리 호환성, 드라이버 호환성을 사전 검증하고 롤백 계획을 준비합니다."

#### 꼬리 질문
- pg_upgrade vs 덤프/리스토어?
- 다운타임 최소화?

---

### Q102. 장애 복구 아키텍처는?

#### A. 핵심 요약
장애 시나리오별 복구 전략과 자동화를 구축합니다.

#### 상세 설명
- HA 클러스터
- 자동 페일오버
- 재해 복구 사이트

#### 실무 예시
- Patroni + etcd
- AWS Multi-AZ

#### 면접 답변 예시
**1분**: "HA 클러스터와 자동 페일오버로 RTO를 최소화합니다."

**3분**: "정기 복구 훈련으로 실제 장애 시 대응 능력을 검증합니다."

#### 꼬리 질문
- 스플릿 브레인 방지?
- DR 사이트 동기화?

---

### Q103. 성능 용량 계획은?

#### A. 핵심 요약
미래 부하를 예측하고 리소스를 계획합니다.

#### 상세 설명
- 성장률 분석
- 병목 예측
- 확장 시점 결정

#### 실무 예시
- 연간 데이터 증가율
- 피크 트래픽 대비

#### 면접 답변 예시
**1분**: "현재 사용률과 성장률을 분석하여 확장 시점을 예측합니다."

**3분**: "QPS, 저장 공간, 연결 수 각각의 한계와 여유를 모니터링합니다."

#### 꼬리 질문
- 버스트 트래픽 대응?
- 수직 vs 수평 확장 결정?

---

### Q104. 대규모 데이터 삭제 전략은?

#### A. 핵심 요약
청크 단위로 나누어 삭제하고 락을 최소화합니다.

#### 상세 설명
- 배치 삭제
- 파티션 드롭
- 복제 지연 모니터링

#### 실무 예시
- 로그 데이터 정리
- GDPR 데이터 삭제

#### 면접 답변 예시
**1분**: "대량 삭제는 청크로 나누어 락 시간을 줄입니다."

**3분**: "파티션된 테이블은 파티션 드롭이 빠릅니다. 복제 지연을 확인하며 진행합니다."

#### 꼬리 질문
- Soft Delete vs Hard Delete?
- 삭제 후 공간 회수?

---

### Q105. 데이터 아카이빙 전략은?

#### A. 핵심 요약
오래된 데이터를 저비용 스토리지로 이동합니다.

#### 상세 설명
- 핫/웜/콜드 계층
- 아카이브 테이블
- S3 Glacier

#### 실무 예시
- 1년 이상 주문 이력
- 감사 로그 보관

#### 면접 답변 예시
**1분**: "접근 빈도에 따라 데이터를 계층화하여 비용을 최적화합니다."

**3분**: "정책에 따라 자동 이동하고, 필요 시 복원할 수 있는 파이프라인을 구축합니다."

#### 꼬리 질문
- 아카이브 데이터 검색?
- 법적 보관 요구사항?

---

### Q106. 쿼리 성능 SLA 관리는?

#### A. 핵심 요약
쿼리 응답 시간 목표를 설정하고 모니터링합니다.

#### 상세 설명
- P50, P95, P99
- 알림 설정
- SLO/SLI 정의

#### 실무 예시
- API 응답 시간 목표
- 대시보드 구축

#### 면접 답변 예시
**1분**: "P95 100ms 같은 SLO를 정의하고 초과 시 알림을 설정합니다."

**3분**: "SLI를 측정하고 에러 버짓을 관리하여 안정성과 개발 속도를 균형 잡습니다."

#### 꼬리 질문
- 에러 버짓이란?
- SLO 위반 시 대응?

---

### Q107. 클라우드 데이터베이스 선택 기준은?

#### A. 핵심 요약
요구사항에 맞는 관리형 서비스와 기능을 비교합니다.

#### 상세 설명
- RDS, Aurora, Cloud SQL
- 관리형 vs 자체 관리
- 비용 모델

#### 실무 예시
- 스타트업: Aurora Serverless
- 대기업: 자체 관리 + 커스터마이징

#### 면접 답변 예시
**1분**: "관리 오버헤드, 성능 요구사항, 비용을 고려하여 선택합니다."

**3분**: "Aurora는 MySQL/PostgreSQL 호환에 자동 확장을 제공합니다. 락인 리스크도 고려해야 합니다."

#### 꼬리 질문
- 벤더 락인 대응?
- 비용 최적화?

---

### Q108. Serverless 데이터베이스란?

#### A. 핵심 요약
사용량에 따라 자동 확장되고 요청당 과금됩니다.

#### 상세 설명
- Aurora Serverless
- DynamoDB 온디맨드
- 콜드 스타트

#### 실무 예시
- 변동 부하 워크로드
- 개발/테스트 환경

#### 면접 답변 예시
**1분**: "서버리스 DB는 프로비저닝 없이 자동 확장되어 운영 부담을 줄입니다."

**3분**: "콜드 스타트 지연이 있고, 예측 가능한 부하에서는 프로비저닝이 더 경제적입니다."

#### 꼬리 질문
- 콜드 스타트 대응?
- 비용 예측?

---

### Q109. 데이터 마스킹과 익명화는?

#### A. 핵심 요약
민감한 데이터를 보호하면서 활용합니다.

#### 상세 설명
- 정적 vs 동적 마스킹
- 토큰화
- 차등 프라이버시

#### 실무 예시
- 개발 환경 데이터
- 분석용 데이터

#### 면접 답변 예시
**1분**: "마스킹은 데이터를 변환하여 원본을 숨기고, 익명화는 개인 식별을 불가능하게 합니다."

**3분**: "개발 환경에는 마스킹된 복사본을 사용하고, 분석에는 차등 프라이버시를 적용합니다."

#### 꼬리 질문
- GDPR 준수?
- 재식별 위험?

---

### Q110. 데이터베이스 감사(Audit)는?

#### A. 핵심 요약
접근과 변경을 기록하여 보안과 규정 준수를 지원합니다.

#### 상세 설명
- 로그인 감사
- 쿼리 감사
- 데이터 변경 감사

#### 실무 예시
- SOC 2 준수
- 이상 접근 탐지

#### 면접 답변 예시
**1분**: "감사 로그로 누가 언제 무엇을 했는지 추적합니다."

**3분**: "성능 영향을 최소화하면서 필요한 이벤트만 기록하고, 중앙 SIEM에 전송합니다."

#### 꼬리 질문
- 감사 로그 보관?
- 성능 영향?

---

### Q111. 인프라 as 코드로 DB 관리는?

#### A. 핵심 요약
Terraform, CloudFormation으로 데이터베이스 인프라를 코드로 관리합니다.

#### 상세 설명
- 버전 관리
- 재현 가능성
- 드리프트 감지

#### 실무 예시
- 환경별 일관성
- PR 리뷰로 변경 검토

#### 면접 답변 예시
**1분**: "IaC로 DB 인프라를 코드로 정의하고 버전 관리합니다."

**3분**: "스키마 마이그레이션은 Flyway, Liquibase로 별도 관리하고, 인프라는 Terraform으로 관리합니다."

#### 꼬리 질문
- 스키마 마이그레이션 도구?
- 시크릿 관리?

---

### Q112. 멀티 클라우드 데이터베이스 전략은?

#### A. 핵심 요약
여러 클라우드에 걸쳐 데이터베이스를 운영합니다.

#### 상세 설명
- 데이터 복제
- 추상화 레이어
- 비용 최적화

#### 실무 예시
- 재해 복구
- 벤더 협상력

#### 면접 답변 예시
**1분**: "멀티 클라우드로 벤더 락인을 줄이고 가용성을 높입니다."

**3분**: "복잡성이 증가하므로 명확한 이유가 있을 때만 선택합니다. 데이터 동기화가 핵심 과제입니다."

#### 꼬리 질문
- 크로스 클라우드 복제?
- 비용 관리?

---

### Q113. 데이터베이스 비용 최적화는?

#### A. 핵심 요약
리소스 사용을 분석하고 효율적으로 관리합니다.

#### 상세 설명
- 예약 인스턴스
- 적정 사이즈 선택
- 스토리지 최적화

#### 실무 예시
- 미사용 인덱스 제거
- 압축 활성화

#### 면접 답변 예시
**1분**: "사용 패턴을 분석하여 적정 사이즈와 예약 인스턴스를 선택합니다."

**3분**: "미사용 인덱스 제거, 데이터 압축, 아카이빙으로 스토리지 비용을 줄입니다."

#### 꼬리 질문
- 비용 모니터링 도구?
- 예약 vs 온디맨드?

---

### Q114. 분산 트랜잭션 패턴은?

#### A. 핵심 요약
서비스 간 데이터 일관성을 유지하는 패턴입니다.

#### 상세 설명
- Saga 패턴
- 2PC
- 최종 일관성

#### 실무 예시
- 주문-재고-결제
- 마이크로서비스

#### 면접 답변 예시
**1분**: "Saga 패턴으로 보상 트랜잭션을 통해 일관성을 유지합니다."

**3분**: "Choreography vs Orchestration Saga를 선택하고, 멱등성을 보장해야 합니다."

#### 꼬리 질문
- Saga 실패 처리?
- 2PC 한계?

---

### Q115. DB 마이그레이션 자동화는?

#### A. 핵심 요약
스키마 변경을 코드로 관리하고 자동 적용합니다.

#### 상세 설명
- 마이그레이션 파일
- 버전 관리
- CI/CD 통합

#### 실무 예시
- Flyway, Liquibase
- Rails Migrations

#### 면접 답변 예시
**1분**: "마이그레이션 파일로 스키마 변경을 버전 관리하고 자동 적용합니다."

**3분**: "롤백 스크립트도 함께 작성하고, 무중단 마이그레이션 패턴을 따릅니다."

#### 꼬리 질문
- 무중단 컬럼 추가?
- 롤백 전략?

---

### Q116. Real-time 분석 아키텍처는?

#### A. 핵심 요약
실시간 데이터를 수집하고 즉시 분석합니다.

#### 상세 설명
- 스트리밍 처리
- OLAP 엔진
- 메트리얼라이즈드 뷰

#### 실무 예시
- 실시간 대시보드
- 이상 탐지

#### 면접 답변 예시
**1분**: "Kafka + ClickHouse로 실시간 데이터를 수집하고 분석합니다."

**3분**: "OLTP에서 CDC로 변경을 캡처하고, OLAP 엔진에서 집계 뷰를 유지합니다."

#### 꼬리 질문
- ClickHouse vs Druid?
- 지연 시간 최적화?

---

### Q117. 데이터베이스 테스트 전략은?

#### A. 핵심 요약
스키마, 데이터, 성능을 자동화하여 테스트합니다.

#### 상세 설명
- 유닛 테스트
- 통합 테스트
- 성능 테스트

#### 실무 예시
- Testcontainers
- 로드 테스트

#### 면접 답변 예시
**1분**: "Testcontainers로 실제 DB에서 통합 테스트를 실행합니다."

**3분**: "마이그레이션 테스트, 제약조건 테스트, 쿼리 성능 테스트를 CI에 포함합니다."

#### 꼬리 질문
- 테스트 데이터 관리?
- 성능 회귀 테스트?

---

### Q118. 하이브리드 트랜잭션/분석 처리(HTAP)란?

#### A. 핵심 요약
하나의 시스템에서 OLTP와 OLAP를 동시에 처리합니다.

#### 상세 설명
- 실시간 분석
- 데이터 복제 제거
- TiDB, SingleStore

#### 실무 예시
- 운영 분석
- 실시간 리포트

#### 면접 답변 예시
**1분**: "HTAP는 별도 ETL 없이 운영 DB에서 바로 분석할 수 있습니다."

**3분**: "컬럼 스토어와 행 스토어를 함께 사용하여 두 워크로드를 지원합니다."

#### 꼬리 질문
- HTAP 한계?
- 워크로드 격리?

---

### Q119. AI/ML을 위한 데이터베이스 최적화는?

#### A. 핵심 요약
대규모 학습 데이터 접근과 피처 저장을 최적화합니다.

#### 상세 설명
- 피처 스토어
- 벡터 데이터베이스
- 배치 조회 최적화

#### 실무 예시
- Feast, Tecton
- Pinecone, Milvus

#### 면접 답변 예시
**1분**: "피처 스토어로 학습과 서빙에 일관된 피처를 제공합니다."

**3분**: "벡터 DB는 임베딩 유사도 검색에 특화되어 있습니다. pgvector로 PostgreSQL에서도 지원됩니다."

#### 꼬리 질문
- ANN 검색이란?
- 피처 버전 관리?

---

### Q120. 데이터베이스 기술 로드맵은?

#### A. 핵심 요약
조직의 데이터 요구사항에 맞는 기술 방향을 설정합니다.

#### 상세 설명
- 기술 부채 평가
- 신기술 검토
- 마이그레이션 계획

#### 실무 예시
- 모놀리스 → 마이크로서비스
- 온프레미스 → 클라우드

#### 면접 답변 예시
**1분**: "현재 상태를 평가하고 비즈니스 요구에 맞는 기술 방향을 설정합니다."

**3분**: "점진적 마이그레이션으로 리스크를 줄이고, PoC로 신기술을 검증합니다."

#### 꼬리 질문
- 기술 선택 기준?
- 레거시 시스템 현대화?