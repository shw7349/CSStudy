# ⚙️ 운영체제 (Operating System) - CS 문제은행

> 총 100문제 | Level 1~4

---

## 📖 주요 CS 용어 정리

### 프로세스 관련
- **프로세스(Process)**: 실행 중인 프로그램. 독립된 메모리 공간(코드, 데이터, 힙, 스택)을 가짐
- **스레드(Thread)**: 프로세스 내의 실행 단위. 프로세스 자원을 공유하며 스택만 독립적으로 가짐
- **PCB(Process Control Block)**: 프로세스 정보를 저장하는 자료구조 (PID, 상태, 레지스터, 메모리 정보 등)
- **컨텍스트 스위칭(Context Switching)**: CPU가 현재 프로세스에서 다른 프로세스로 전환하는 과정

### 메모리 관리
- **가상 메모리(Virtual Memory)**: 물리 메모리보다 큰 메모리를 사용할 수 있게 하는 기술
- **페이징(Paging)**: 가상 메모리를 고정 크기(페이지) 단위로 나누어 관리
- **세그멘테이션(Segmentation)**: 논리적 단위(코드, 데이터, 스택)로 메모리를 나누어 관리
- **스와핑(Swapping)**: 메모리가 부족할 때 프로세스를 디스크로 옮기는 기법
- **페이지 폴트(Page Fault)**: 필요한 페이지가 메모리에 없을 때 발생하는 인터럽트

### 스케줄링
- **스케줄러(Scheduler)**: CPU에 실행할 프로세스를 선택하는 OS 구성 요소
- **선점형(Preemptive)**: 실행 중인 프로세스를 강제로 중단시킬 수 있는 스케줄링
- **비선점형(Non-preemptive)**: 프로세스가 자발적으로 CPU를 반환할 때까지 대기
- **멀티태스킹(Multitasking)**: 여러 프로세스를 동시에 실행하는 것처럼 보이게 하는 기술
- **타임 슬라이스(Time Slice)**: 각 프로세스에 할당되는 CPU 사용 시간

### 동기화
- **임계 영역(Critical Section)**: 동시에 접근하면 안 되는 공유 자원 영역
- **뮤텍스(Mutex)**: 상호 배제를 위한 동기화 객체 (한 번에 하나의 스레드만 접근)
- **세마포어(Semaphore)**: 카운터를 이용한 동기화 기법 (N개까지 접근 허용)
- **데드락(Deadlock)**: 두 개 이상의 프로세스가 서로의 자원을 기다리며 무한 대기
- **경쟁 상태(Race Condition)**: 여러 프로세스가 동시에 공유 자원에 접근할 때 발생하는 문제

### 시스템 호출
- **커널(Kernel)**: 운영체제의 핵심 부분. 하드웨어와 직접 상호작용
- **시스템 콜(System Call)**: 사용자 프로그램이 커널 기능을 요청하는 인터페이스
- **인터럽트(Interrupt)**: 하드웨어나 소프트웨어가 CPU에게 즉시 처리를 요청하는 신호
- **커널 모드(Kernel Mode)**: 모든 자원에 접근 가능한 특권 모드
- **사용자 모드(User Mode)**: 제한된 권한으로 실행되는 일반 애플리케이션 모드

### 🎯 시니어급 고급 용어

#### CPU & 성능 최적화
- **NUMA(Non-Uniform Memory Access)**: CPU마다 가까운 메모리와 먼 메모리가 있어 접근 속도가 다른 아키텍처
- **CPU 캐시 일관성(Cache Coherence)**: 멀티코어 환경에서 각 CPU 캐시 간 데이터 일관성을 유지하는 메커니즘
- **메모리 배리어(Memory Barrier)**: CPU가 명령어 재배치를 방지하여 메모리 순서를 보장하는 명령어
- **False Sharing**: 여러 스레드가 같은 캐시 라인의 다른 변수를 수정하여 성능 저하가 발생하는 현상
- **Copy-on-Write(COW)**: fork() 시 메모리를 복사하지 않고 쓰기 발생 시에만 복사하는 최적화 기법
- **Zero-Copy**: 커널과 사용자 공간 간 데이터 복사를 최소화하여 I/O 성능을 향상시키는 기법

#### 고급 동기화
- **Spinlock**: 대기 중인 스레드가 CPU를 계속 사용하며 락을 폴링하는 동기화 기법 (짧은 대기 시간에 유리)
- **RCU(Read-Copy-Update)**: 읽기 성능을 극대화한 동기화 기법. 업데이트 시 복사본을 만들어 수정
- **Lock-free 알고리즘**: 락 없이 Compare-And-Swap(CAS) 같은 원자적 연산으로 동기화
- **Wait-free 알고리즘**: 모든 스레드가 유한 시간 내에 작업을 완료하도록 보장하는 알고리즘
- **우선순위 역전(Priority Inversion)**: 낮은 우선순위 프로세스가 락을 잡아 높은 우선순위가 대기하는 현상
- **Reader-Writer Lock**: 읽기는 동시 허용, 쓰기는 배타적 접근만 허용하는 락
- **Barrier Synchronization**: 여러 스레드가 특정 지점에서 모두 도달할 때까지 대기
- **Live Lock**: 두 프로세스가 서로를 위해 양보하며 진행하지 못하는 상태 (데드락과 유사하지만 상태는 계속 변함)
- **Starvation**: 특정 프로세스가 계속 우선순위에서 밀려 실행되지 못하는 현상

##### 심화 동시성 문제
- **Cache Stampede(Thundering Herd)**: 캐시 만료 시 동시에 대량의 요청이 DB로 몰려 부하 급증하는 현상
  - **해결책**: 락 기반 재생성, 확률적 조기 만료, 백그라운드 갱신
- **ABA Problem**: CAS 연산 시 A→B→A로 값이 변했다가 되돌아와 변경을 감지 못하는 문제
  - **해결책**: Version Number, Tagged Pointer 사용
- **Double-Checked Locking**: 성능 최적화를 위해 락 전후 두 번 체크 (싱글톤 패턴에서 자주 사용)
  - **주의**: 메모리 재배치로 인한 문제 발생 가능 (volatile 필요)
- **Memory Fence(Memory Barrier)**: 특정 지점 이전/이후의 메모리 연산 순서를 보장하는 명령어
  - **종류**: Load Fence, Store Fence, Full Fence
- **Happens-Before**: 멀티스레드에서 메모리 연산의 가시성 순서 관계 (Java Memory Model 핵심)
- **Linearizability(선형화 가능성)**: 동시 연산이 순차적 실행처럼 보이도록 보장하는 일관성 모델 (가장 강한 일관성)
- **Sequential Consistency**: 모든 스레드가 동일한 연산 순서를 관찰하는 일관성 모델
- **Convoy Effect**: 짧은 임계 영역을 가진 프로세스들이 긴 임계 영역 뒤에서 대기하며 성능 저하
- **Lock Contention**: 여러 스레드가 동일한 락을 획득하려고 경쟁하여 성능 저하
- **False Sharing**: 여러 스레드가 같은 캐시 라인의 다른 변수를 수정하여 캐시 무효화 반복 (이미 CPU 섹션에 있지만 동기화와도 관련)
- **Thundering Herd (일반)**: 여러 프로세스가 동일한 이벤트를 대기하다가 동시에 깨어나 리소스 경쟁
  - **예시**: accept() 시스템 콜, epoll_wait()에서 발생 가능

#### I/O & 비동기 처리
- **epoll**: Linux의 고성능 I/O 이벤트 알림 메커니즘 (O(1) 복잡도)
- **kqueue**: BSD 계열의 이벤트 알림 메커니즘
- **IOCP(I/O Completion Port)**: Windows의 비동기 I/O 완료 알림 메커니즘
- **AIO(Asynchronous I/O)**: 비동기 I/O 작업. 완료를 대기하지 않고 다른 작업 수행
- **DMA(Direct Memory Access)**: CPU를 거치지 않고 메모리와 I/O 장치 간 직접 데이터 전송
- **MMIO(Memory-Mapped I/O)**: I/O 장치를 메모리 주소 공간에 매핑하여 접근

#### 컨테이너 & 가상화
- **cgroup(Control Groups)**: 리소스 사용량을 제한하고 격리하는 Linux 커널 기능
- **namespace**: 프로세스 격리를 위한 Linux 커널 기능 (PID, Network, Mount, User 등)
- **Hypervisor**: 가상 머신을 생성하고 관리하는 소프트웨어 (Type 1: 하드웨어 직접, Type 2: OS 위)
- **Paravirtualization**: 게스트 OS가 가상화를 인지하고 Hypervisor와 협력하는 방식

#### 메모리 고급
- **TLB(Translation Lookaside Buffer)**: 가상 주소-물리 주소 변환을 캐싱하는 하드웨어
- **Huge Pages**: 일반 4KB보다 큰 페이지(2MB, 1GB)를 사용하여 TLB 미스를 줄이는 기법
- **Memory Compaction**: 메모리 단편화를 줄이기 위해 페이지를 재배치하는 기법
- **OOM Killer**: 메모리 부족 시 프로세스를 강제 종료하여 시스템을 보호하는 Linux 메커니즘
- **Working Set**: 프로세스가 일정 시간 동안 참조하는 페이지 집합

#### 가비지 컬렉션(GC)
- **Garbage Collection(GC)**: 프로그램이 동적으로 할당한 메모리 중 더 이상 사용하지 않는 메모리를 자동으로 회수하는 메커니즘
- **Stop-the-World(STW)**: GC 실행 동안 애플리케이션의 모든 스레드를 일시 중지하는 현상
- **GC Roots**: GC가 추적을 시작하는 객체들 (스택의 로컬 변수, 정적 변수, JNI 참조 등)
- **Reachability**: 객체가 GC Root로부터 참조 체인을 통해 도달 가능한지 여부

##### GC 알고리즘
- **Mark and Sweep**: 도달 가능한 객체를 마킹(Mark)하고, 마킹되지 않은 객체를 회수(Sweep)
- **Mark and Compact**: Mark & Sweep 후 살아있는 객체를 메모리 한쪽으로 압축하여 단편화 해소
- **Copying GC**: 메모리를 두 영역으로 나누고, 살아있는 객체를 다른 영역으로 복사한 후 원래 영역 전체 회수
- **Reference Counting**: 객체의 참조 횟수를 추적하여 0이 되면 즉시 회수 (순환 참조 문제)
- **Tri-color Marking**: 흰색(미방문), 회색(방문했지만 자식 미방문), 검은색(완전히 방문)으로 구분하여 마킹

##### Generational GC
- **Generational Hypothesis**: 대부분의 객체는 생성 직후 죽는다는 가설
- **Young Generation**: 새로 생성된 객체가 할당되는 영역 (Minor GC 발생)
- **Old Generation(Tenured)**: Young에서 오래 살아남은 객체가 이동하는 영역 (Major GC 발생)
- **Eden Space**: Young Generation 내에서 객체가 최초 할당되는 공간
- **Survivor Space**: Eden에서 GC 후 살아남은 객체가 이동하는 공간 (S0, S1 두 개 존재)
- **Minor GC**: Young Generation에서 발생하는 GC (빠르고 빈번함)
- **Major GC(Full GC)**: Old Generation을 포함한 전체 힙 GC (느리고 STW 시간 김)
- **Promotion(Tenuring)**: 객체가 Young에서 Old로 이동하는 과정
- **Premature Promotion**: 젊은 객체가 너무 빨리 Old로 승격되어 Major GC 빈도가 증가하는 현상

##### 고급 GC 기법
- **Concurrent GC**: 애플리케이션 스레드와 GC 스레드가 동시에 실행 (STW 시간 최소화)
- **Incremental GC**: GC 작업을 여러 단계로 나누어 수행하여 일시 중지 시간 분산
- **Parallel GC**: 여러 스레드가 동시에 GC 수행 (처리량 향상)
- **Card Table**: Old → Young 참조를 효율적으로 추적하기 위한 자료구조
- **Remembered Set**: 특정 영역을 참조하는 외부 포인터를 기록하는 자료구조
- **Write Barrier**: 객체 참조 변경 시 GC가 이를 추적할 수 있도록 삽입된 코드
- **Safe Point**: GC가 안전하게 실행될 수 있는 프로그램 지점 (메서드 호출, 루프 백엣지 등)

##### 주요 GC 구현체 (JVM 기준)
- **Serial GC**: 단일 스레드로 동작. 작은 힙과 단일 CPU 환경에 적합
- **Parallel GC(Throughput GC)**: 여러 스레드로 병렬 수행. 처리량 최대화
- **CMS(Concurrent Mark Sweep)**: 애플리케이션과 동시 실행. 낮은 지연 시간 (Java 14에서 deprecated)
- **G1 GC(Garbage First)**: 힙을 리전으로 나누고 가비지가 많은 리전부터 회수. 예측 가능한 STW 시간
- **ZGC**: 매우 큰 힙(TB급)에서도 10ms 이하 STW 보장. Colored Pointers 사용
- **Shenandoah**: Red Hat이 개발한 초저지연 GC. G1과 유사하지만 더 낮은 지연 시간

##### GC 튜닝 & 모니터링
- **GC Overhead**: 전체 실행 시간 중 GC에 소요된 시간 비율
- **Heap Sizing**: Young/Old 비율, 전체 힙 크기 조정 (-Xms, -Xmx, -XX:NewRatio)
- **GC Log Analysis**: GC 로그를 분석하여 병목 지점 파악
- **Memory Leak**: GC로 회수되지 않고 계속 누적되는 메모리
- **Metaspace**: 클래스 메타데이터를 저장하는 네이티브 메모리 영역 (Java 8+, PermGen 대체)
- **Weak/Soft/Phantom Reference**: 다양한 강도의 참조 타입. 캐시나 리소스 관리에 사용
- **Finalization**: 객체가 회수되기 전 `finalize()` 메서드 호출 (성능 문제로 비권장)

##### 다른 언어의 GC
- **Python**: Reference Counting + Cycle Detector (순환 참조 감지)
- **Go**: Concurrent Mark & Sweep. 매우 짧은 STW 시간(<1ms) 목표
- **JavaScript(V8)**: Generational GC (Scavenger + Mark-Compact)
- **Rust**: 소유권 시스템으로 GC 없이 메모리 안전성 보장
- **C#(.NET)**: Generational GC with Server/Workstation 모드

#### 파일 시스템 고급
- **Journaling**: 파일 시스템 변경 사항을 로그에 기록하여 장애 시 복구를 보장 (ext4, XFS)
- **Copy-on-Write 파일 시스템**: 수정 시 원본을 보존하고 새 블록에 쓰는 방식 (Btrfs, ZFS)
- **inode**: 파일 메타데이터를 저장하는 자료구조 (권한, 크기, 타임스탬프, 블록 포인터)
- **VFS(Virtual File System)**: 다양한 파일 시스템에 대한 통일된 인터페이스 제공

#### 실시간 시스템
- **Hard Real-Time**: 데드라인을 절대 놓치지 않아야 하는 시스템 (항공, 의료)
- **Soft Real-Time**: 데드라인을 대부분 지키지만 가끔 놓쳐도 되는 시스템 (멀티미디어)
- **Priority Ceiling**: 우선순위 역전을 방지하기 위해 락의 우선순위 상한을 설정

---

## Level 1 - 기초 (1~2년차) | 25문제

---

### Q1. 운영체제란?

#### A. 핵심 요약
하드웨어와 사용자 사이에서 자원을 관리하고 서비스를 제공하는 시스템 소프트웨어입니다.

#### 상세 설명
- 프로세스, 메모리, 파일, I/O 관리
- 사용자 인터페이스 제공
- 하드웨어 추상화

#### 실무 예시
- Windows, Linux, macOS
- 서버 운영체제 선택
- 컨테이너 vs VM

#### 면접 답변 예시
**1분**: "운영체제는 하드웨어 자원을 관리하고 응용 프로그램에 실행 환경을 제공합니다."

**3분**: "CPU 스케줄링, 메모리 관리, 파일 시스템, 장치 드라이버 등을 담당합니다. 커널은 핵심 기능을, 셸은 사용자 인터페이스를 제공합니다."

#### 꼬리 질문
- 커널이란?
- 마이크로커널 vs 모놀리식?

---

### Q2. 프로세스와 스레드의 차이는?

#### A. 핵심 요약
프로세스는 독립된 메모리 공간을 가진 실행 단위, 스레드는 프로세스 내 자원을 공유하는 실행 흐름입니다.

#### 상세 설명
- 프로세스: 독립 주소 공간, IPC 필요
- 스레드: 코드, 데이터, 힙 공유, 스택만 독립
- 컨텍스트 스위칭 비용 차이

#### 실무 예시
- 웹 서버: 멀티스레드
- 브라우저: 멀티프로세스 (탭별)
- 크롬의 프로세스 격리

#### 면접 답변 예시
**1분**: "프로세스는 독립 메모리, 스레드는 메모리 공유로 가볍고 빠르지만 동기화가 필요합니다."

**3분**: "스레드는 생성/전환 비용이 낮지만 하나의 오류가 전체에 영향을 줄 수 있습니다. 프로세스는 격리되어 안정적이지만 IPC 오버헤드가 있습니다."

#### 꼬리 질문
- 멀티프로세스 vs 멀티스레드 선택 기준?
- 스레드 안전이란?

---

### Q3. 프로세스 상태와 전이는?

#### A. 핵심 요약
생성→준비→실행→대기→종료 상태를 거치며 스케줄러가 전이를 관리합니다.

#### 상세 설명
- New: 생성 중
- Ready: CPU 할당 대기
- Running: 실행 중
- Waiting/Blocked: I/O 등 대기
- Terminated: 종료

#### 실무 예시
- ps 명령어로 상태 확인
- 좀비 프로세스, 고아 프로세스

#### 면접 답변 예시
**1분**: "프로세스는 준비-실행-대기를 순환하며, 스케줄러가 CPU를 할당합니다."

**3분**: "타임 슬라이스 만료 시 실행→준비, I/O 요청 시 실행→대기, I/O 완료 시 대기→준비로 전이합니다."

#### 꼬리 질문
- 좀비 프로세스란?
- 고아 프로세스란?

---

### Q4. PCB(Process Control Block)란?

#### A. 핵심 요약
프로세스 정보를 저장하는 커널 자료구조입니다.

#### 상세 설명
- PID, 상태, 프로그램 카운터
- 레지스터, 메모리 정보
- 스케줄링 정보, I/O 상태

#### 실무 예시
- 컨텍스트 스위칭 시 PCB 저장/복원
- /proc 파일 시스템

#### 면접 답변 예시
**1분**: "PCB는 프로세스의 모든 실행 정보를 담아 컨텍스트 스위칭에 사용됩니다."

**3분**: "커널이 각 프로세스마다 PCB를 유지하며, 스케줄링, 메모리 관리, 인터럽트 처리에 활용합니다."

#### 꼬리 질문
- 컨텍스트 스위칭 과정?
- TCB란?

---

### Q5. 컨텍스트 스위칭이란?

#### A. 핵심 요약
CPU가 한 프로세스에서 다른 프로세스로 전환할 때 상태를 저장/복원하는 것입니다.

#### 상세 설명
- 현재 프로세스 상태를 PCB에 저장
- 다음 프로세스 상태를 PCB에서 복원
- 오버헤드 발생

#### 실무 예시
- 스레드 풀로 스위칭 최소화
- 코루틴의 경량 스위칭

#### 면접 답변 예시
**1분**: "컨텍스트 스위칭은 레지스터, PC 등을 저장/복원하며 수 마이크로초가 소요됩니다."

**3분**: "캐시 무효화, TLB 플러시 등 간접 비용도 있습니다. 스레드 간 전환이 프로세스 간보다 빠릅니다."

#### 꼬리 질문
- 컨텍스트 스위칭 비용 측정 방법?
- 유저 레벨 스레드의 장점?

---

### Q6. CPU 스케줄링이란?

#### A. 핵심 요약
여러 프로세스에 CPU를 할당하는 정책과 메커니즘입니다.

#### 상세 설명
- 선점형 vs 비선점형
- 스케줄링 기준: CPU 이용률, 처리량, 대기시간
- 장기, 중기, 단기 스케줄러

#### 실무 예시
- 서버 워크로드 최적화
- 실시간 시스템 스케줄링

#### 면접 답변 예시
**1분**: "CPU 스케줄링은 여러 프로세스 중 다음 실행할 것을 선택하는 정책입니다."

**3분**: "선점형은 타임 슬라이스로 강제 전환하고, 비선점형은 프로세스가 자발적으로 양보합니다."

#### 꼬리 질문
- 선점형 vs 비선점형?
- 디스패처란?

---

### Q7. 스케줄링 알고리즘들은?

#### A. 핵심 요약
FCFS, SJF, Round Robin, Priority, Multilevel Queue 등이 있습니다.

#### 상세 설명
- FCFS: 먼저 온 순서, Convoy 효과
- SJF: 최단 작업 우선, 평균 대기시간 최소
- RR: 타임 슬라이스, 공정성
- Priority: 우선순위 기반

#### 실무 예시
- Linux CFS
- Windows 스케줄러

#### 면접 답변 예시
**1분**: "RR은 공정하고, SJF는 평균 대기시간이 최소지만 기아 가능성이 있습니다."

**3분**: "실제 OS는 여러 알고리즘을 조합합니다. CFS는 가상 실행 시간으로 공정성을 보장합니다."

#### 꼬리 질문
- CFS란?
- 기아(Starvation) 해결 방법?

---

### Q8. 교착상태(Deadlock)란?

#### A. 핵심 요약
두 개 이상의 프로세스가 서로의 자원을 기다리며 무한 대기하는 상태입니다.

#### 상세 설명
- 4가지 필요 조건: 상호배제, 점유대기, 비선점, 순환대기
- 예방, 회피, 탐지, 복구 전략

#### 실무 예시
- 데이터베이스 락
- 멀티스레드 동기화

#### 면접 답변 예시
**1분**: "교착상태는 4가지 조건이 동시에 만족될 때 발생하며, 하나라도 깨면 예방됩니다."

**3분**: "은행원 알고리즘으로 안전 상태를 유지하거나, 타임아웃으로 탐지 후 복구합니다."

#### 꼬리 질문
- 은행원 알고리즘이란?
- 데드락 vs 라이브락?

---

### Q9. 동기화란?

#### A. 핵심 요약
여러 프로세스/스레드가 공유 자원에 안전하게 접근하도록 조율하는 것입니다.

#### 상세 설명
- 경쟁 조건(Race Condition) 방지
- 임계 구역(Critical Section) 보호
- 상호 배제 보장

#### 실무 예시
- 뮤텍스, 세마포어
- synchronized 키워드

#### 면접 답변 예시
**1분**: "동기화는 공유 자원 접근을 조율하여 경쟁 조건을 방지합니다."

**3분**: "임계 구역은 한 번에 하나의 프로세스만 진입해야 합니다. 진행, 한정 대기 조건도 만족해야 합니다."

#### 꼬리 질문
- 임계 구역 조건 3가지?
- 경쟁 조건 예시?

---

### Q10. 뮤텍스와 세마포어 차이는?

#### A. 핵심 요약
뮤텍스는 이진(0/1) 락, 세마포어는 카운팅 동기화 도구입니다.

#### 상세 설명
- 뮤텍스: 소유권 개념, 락한 스레드만 해제
- 세마포어: 카운터, N개 동시 접근 허용
- 이진 세마포어 ≈ 뮤텍스

#### 실무 예시
- DB 연결 풀 (세마포어)
- 파일 단독 접근 (뮤텍스)

#### 면접 답변 예시
**1분**: "뮤텍스는 단일 접근 보장, 세마포어는 N개 동시 접근을 제어합니다."

**3분**: "뮤텍스는 소유권이 있어 데드락 탐지가 쉽습니다. 세마포어는 생산자-소비자 패턴에 적합합니다."

#### 꼬리 질문
- 이진 세마포어 vs 뮤텍스?
- 재진입 뮤텍스란?

---

### Q11. 가상 메모리란?

#### A. 핵심 요약
물리 메모리보다 큰 주소 공간을 제공하는 메모리 관리 기법입니다.

#### 상세 설명
- 페이지 단위로 물리 메모리/디스크 매핑
- 요구 페이징으로 필요시 로드
- 페이지 테이블로 주소 변환

#### 실무 예시
- 스왑 공간
- 메모리 맵 파일

#### 면접 답변 예시
**1분**: "가상 메모리는 디스크를 활용해 물리 메모리 이상의 공간을 제공합니다."

**3분**: "프로세스마다 독립 주소 공간을 제공하고, 필요한 페이지만 메모리에 올려 효율적입니다."

#### 꼬리 질문
- 요구 페이징이란?
- 스와핑이란?

---

### Q12. 페이징과 세그멘테이션 차이는?

#### A. 핵심 요약
페이징은 고정 크기 블록, 세그멘테이션은 가변 크기 논리 단위입니다.

#### 상세 설명
- 페이징: 외부 단편화 없음, 내부 단편화 있음
- 세그멘테이션: 논리적 구분, 외부 단편화 있음
- 현대 OS는 페이징 주로 사용

#### 실무 예시
- x86 페이지 크기 4KB
- 코드/데이터/스택 세그먼트

#### 면접 답변 예시
**1분**: "페이징은 고정 크기로 외부 단편화 없고, 세그멘테이션은 논리 단위로 구분합니다."

**3분**: "현대 OS는 페이징을 기본으로, 세그멘테이션은 보호 목적으로 제한적 사용합니다."

#### 꼬리 질문
- 내부 vs 외부 단편화?
- 페이지 테이블 구조?

---

### Q13. 페이지 교체 알고리즘은?

#### A. 핵심 요약
메모리가 부족할 때 어떤 페이지를 교체할지 결정합니다.

#### 상세 설명
- FIFO: 먼저 들어온 페이지
- LRU: 가장 오래 미사용
- Clock: LRU 근사

#### 실무 예시
- Linux의 페이지 교체
- 캐시 교체 정책

#### 면접 답변 예시
**1분**: "LRU가 이론적 최적에 가깝지만, 구현 비용으로 Clock 알고리즘을 사용합니다."

**3분**: "Belady의 최적 알고리즘은 미래를 알아야 하므로 비교 기준으로만 사용합니다."

#### 꼬리 질문
- Belady's Anomaly란?
- Second Chance 알고리즘?

---

### Q14. 스레싱(Thrashing)이란?

#### A. 핵심 요약
페이지 폴트가 과도하게 발생하여 CPU가 대부분 페이징에 소비되는 상태입니다.

#### 상세 설명
- 워킹셋보다 적은 메모리 할당 시 발생
- CPU 이용률 급감
- 프로세스 수 조절로 해결

#### 실무 예시
- 메모리 부족 시 성능 저하
- OOM Killer

#### 면접 답변 예시
**1분**: "스레싱은 페이지 폴트 처리에 CPU가 소비되어 실제 작업을 못하는 상태입니다."

**3분**: "워킹셋 모델로 프로세스별 필요 페이지 수를 추정하고, 부족하면 프로세스를 스왑아웃합니다."

#### 꼬리 질문
- 워킹셋이란?
- OOM Killer란?

---

### Q15. 파일 시스템이란?

#### A. 핵심 요약
디스크의 파일을 조직하고 관리하는 OS 구성요소입니다.

#### 상세 설명
- 파일, 디렉토리, 메타데이터 관리
- 디스크 블록 할당
- 접근 제어

#### 실무 예시
- ext4, NTFS, APFS
- 저널링 파일 시스템

#### 면접 답변 예시
**1분**: "파일 시스템은 디스크의 데이터를 파일/디렉토리로 조직하고 관리합니다."

**3분**: "inode에 메타데이터, 데이터 블록에 실제 내용을 저장합니다. 저널링으로 일관성을 보장합니다."

#### 꼬리 질문
- inode란?
- 저널링이란?

---

### Q16. 인터럽트란?

#### A. 핵심 요약
CPU에 이벤트 발생을 알려 현재 작업을 중단하고 처리하게 하는 메커니즘입니다.

#### 상세 설명
- 하드웨어 인터럽트: 외부 장치
- 소프트웨어 인터럽트: 시스템 콜, 예외
- 인터럽트 벡터 테이블

#### 실무 예시
- 키보드 입력
- 타이머 인터럽트
- 페이지 폴트

#### 면접 답변 예시
**1분**: "인터럽트는 비동기 이벤트를 처리하기 위해 CPU 실행을 중단시킵니다."

**3분**: "인터럽트 발생 시 컨텍스트를 저장하고 핸들러를 실행한 후 복귀합니다. 우선순위로 중첩 처리합니다."

#### 꼬리 질문
- 폴링 vs 인터럽트?
- Top Half vs Bottom Half?

---

### Q17. 시스템 콜이란?

#### A. 핵심 요약
사용자 프로그램이 커널 서비스를 요청하는 인터페이스입니다.

#### 상세 설명
- 유저 모드 → 커널 모드 전환
- 소프트웨어 인터럽트/트랩 사용
- 프로세스, 파일, 메모리 관련 시스템 콜

#### 실무 예시
- open, read, write, fork, exec
- strace로 시스템 콜 추적

#### 면접 답변 예시
**1분**: "시스템 콜은 커널 기능을 안전하게 호출하는 프로그래밍 인터페이스입니다."

**3분**: "레지스터에 인자를 넣고 트랩 명령어로 커널 모드로 전환합니다. 라이브러리 함수가 시스템 콜을 래핑합니다."

#### 꼬리 질문
- 유저 모드 vs 커널 모드?
- strace 사용법?

---

### Q18. 사용자 모드와 커널 모드 차이는?

#### A. 핵심 요약
커널 모드는 모든 명령어/메모리 접근 가능, 사용자 모드는 제한됩니다.

#### 상세 설명
- 보호 링(Protection Ring)
- 특권 명령어는 커널 모드만
- 모드 비트로 구분

#### 실무 예시
- I/O 명령어
- 메모리 보호

#### 면접 답변 예시
**1분**: "커널 모드는 하드웨어 직접 제어, 사용자 모드는 시스템 콜로만 커널 접근합니다."

**3분**: "잘못된 사용자 코드가 시스템을 손상시키지 않도록 보호합니다. x86은 4개 링을 지원하지만 대부분 2개만 사용합니다."

#### 꼬리 질문
- Ring 0, Ring 3?
- SMAP, SMEP란?

---

### Q19. IPC(프로세스 간 통신)란?

#### A. 핵심 요약
독립된 프로세스 간 데이터를 교환하는 메커니즘입니다.

#### 상세 설명
- 파이프, 메시지 큐
- 공유 메모리
- 소켓, 시그널

#### 실무 예시
- 쉘 파이프 (|)
- Redis, RabbitMQ
- Unix 소켓

#### 면접 답변 예시
**1분**: "IPC는 프로세스 간 데이터 교환으로, 파이프, 공유메모리, 소켓 등이 있습니다."

**3분**: "공유 메모리가 가장 빠르지만 동기화가 필요합니다. 메시지 패싱은 동기화가 내장되어 있습니다."

#### 꼬리 질문
- 익명 파이프 vs 명명 파이프?
- mmap 공유 메모리?

---

### Q20. 캐시 메모리란?

#### A. 핵심 요약
CPU와 메인 메모리 사이의 고속 버퍼로 접근 속도를 높입니다.

#### 상세 설명
- L1, L2, L3 캐시 계층
- 시간 지역성, 공간 지역성
- 캐시 히트/미스

#### 실무 예시
- 캐시 친화적 코드 작성
- False Sharing 문제

#### 면접 답변 예시
**1분**: "캐시는 자주 접근하는 데이터를 빠른 메모리에 유지하여 성능을 높입니다."

**3분**: "L1은 수 사이클, 메인 메모리는 수백 사이클입니다. 캐시 라인 크기(64B)를 고려한 데이터 배치가 중요합니다."

#### 꼬리 질문
- 캐시 일관성이란?
- False Sharing이란?

---

### Q21. 메모리 관리 기법들은?

#### A. 핵심 요약
연속 할당, 페이징, 세그멘테이션 등으로 메모리를 관리합니다.

#### 상세 설명
- 고정 분할, 가변 분할
- 페이징, 세그멘테이션
- 버디 시스템

#### 실무 예시
- malloc/free
- slab allocator

#### 면접 답변 예시
**1분**: "페이징은 외부 단편화를, 버디 시스템은 빠른 할당/해제를 제공합니다."

**3분**: "커널은 버디 시스템으로 페이지를 관리하고, slab allocator로 작은 객체를 효율적으로 할당합니다."

#### 꼬리 질문
- 버디 시스템이란?
- Slab Allocator란?

---

### Q22. 동기 vs 비동기 I/O란?

#### A. 핵심 요약
동기는 완료까지 대기, 비동기는 요청 후 다른 작업을 계속합니다.

#### 상세 설명
- 동기: 블로킹, 단순함
- 비동기: 논블로킹, 효율적
- I/O 멀티플렉싱

#### 실무 예시
- epoll, kqueue
- Node.js 이벤트 루프

#### 면접 답변 예시
**1분**: "비동기 I/O는 I/O 대기 중 CPU를 활용하여 처리량을 높입니다."

**3분**: "select/poll/epoll로 여러 I/O를 동시 대기합니다. 콜백이나 Promise로 완료를 처리합니다."

#### 꼬리 질문
- select vs epoll?
- Reactor 패턴이란?

---

### Q23. DMA(Direct Memory Access)란?

#### A. 핵심 요약
CPU 개입 없이 장치가 메모리와 직접 데이터를 전송하는 방식입니다.

#### 상세 설명
- CPU 부하 감소
- 대용량 데이터 전송에 효율적
- DMA 컨트롤러가 관리

#### 실무 예시
- 디스크 I/O
- 네트워크 패킷 전송

#### 면접 답변 예시
**1분**: "DMA는 CPU 대신 장치가 직접 메모리에 접근하여 효율적인 전송을 합니다."

**3분**: "CPU는 DMA 요청만 하고 다른 작업을 수행합니다. 완료 시 인터럽트로 통보받습니다."

#### 꼬리 질문
- PIO vs DMA?
- Zero-Copy란?

---

### Q24. 부팅 과정은?

#### A. 핵심 요약
전원 → BIOS/UEFI → 부트로더 → 커널 → init 순서로 시스템이 시작됩니다.

#### 상세 설명
- POST: 하드웨어 점검
- 부트로더: 커널 로드 (GRUB)
- 커널 초기화, init 프로세스

#### 실무 예시
- GRUB 설정
- systemd 서비스

#### 면접 답변 예시
**1분**: "BIOS가 부트로더를 로드하고, 부트로더가 커널을, 커널이 init을 시작합니다."

**3분**: "UEFI는 BIOS의 현대적 대체로 보안 부팅을 지원합니다. systemd가 서비스들을 병렬로 시작합니다."

#### 꼬리 질문
- BIOS vs UEFI?
- systemd란?

---

### Q25. 메모리 계층 구조란?

#### A. 핵심 요약
레지스터 → 캐시 → RAM → SSD/HDD 순으로 용량 증가, 속도 감소하는 구조입니다.

#### 상세 설명
- 빠를수록 비싸고 작음
- 지역성 원리 활용
- 계층 간 데이터 이동

#### 실무 예시
- 캐시 최적화
- 메모리 vs 디스크 선택

#### 면접 답변 예시
**1분**: "메모리 계층은 속도-용량 트레이드오프를 최적화하여 비용 효율적 성능을 제공합니다."

**3분**: "프로그램의 지역성 덕분에 작은 빠른 메모리로도 큰 느린 메모리처럼 동작합니다."

#### 꼬리 질문
- 시간 지역성 vs 공간 지역성?
- 메모리 접근 시간 비교?

---

## Level 2 - 중급 (3~4년차) | 30문제

---

### Q26. 뮤텍스 구현 방법은?

#### A. 핵심 요약
하드웨어 원자적 명령어(CAS, TAS)와 OS 지원으로 구현합니다.

#### 상세 설명
- Test-and-Set, Compare-and-Swap
- 스핀락 vs 슬립 락
- Futex (Fast Userspace Mutex)

#### 실무 예시
- pthread_mutex
- std::mutex

#### 면접 답변 예시
**1분**: "CAS 명령어로 원자적으로 락 상태를 확인하고 획득합니다."

**3분**: "스핀락은 짧은 대기에, 슬립 락은 긴 대기에 적합합니다. Futex는 경합 없으면 커널 진입 없이 동작합니다."

#### 꼬리 질문
- CAS란?
- Futex 동작 원리?

---

### Q27. 조건 변수(Condition Variable)란?

#### A. 핵심 요약
특정 조건이 만족될 때까지 스레드를 대기시키는 동기화 도구입니다.

#### 상세 설명
- wait: 조건 대기, 락 해제
- signal/broadcast: 대기 스레드 깨움
- 뮤텍스와 함께 사용

#### 실무 예시
- 생산자-소비자 패턴
- pthread_cond_wait

#### 면접 답변 예시
**1분**: "조건 변수는 조건 불만족 시 대기하고, 만족 시 깨워서 효율적 대기를 합니다."

**3분**: "spurious wakeup에 대비해 while 루프로 조건을 재확인해야 합니다."

#### 꼬리 질문
- Spurious Wakeup이란?
- Monitor란?

---

### Q28. 읽기-쓰기 락(RWLock)이란?

#### A. 핵심 요약
읽기는 동시 허용, 쓰기는 배타적으로 허용하는 락입니다.

#### 상세 설명
- 읽기 많은 워크로드에 효율적
- 쓰기 기아 문제 가능
- 공정성 정책 필요

#### 실무 예시
- 캐시 읽기
- 설정 파일 접근

#### 면접 답변 예시
**1분**: "RWLock은 다중 읽기를 허용하여 읽기 위주 작업의 처리량을 높입니다."

**3분**: "쓰기 대기 중에도 새 읽기를 허용하면 쓰기 기아가 발생합니다. 쓰기 우선 정책으로 해결합니다."

#### 꼬리 질문
- 쓰기 기아 해결법?
- SeqLock이란?

---

### Q29. 스핀락 vs 뮤텍스 선택 기준은?

#### A. 핵심 요약
짧은 대기는 스핀락, 긴 대기는 뮤텍스가 효율적입니다.

#### 상세 설명
- 스핀락: CPU 사용하며 대기
- 뮤텍스: 슬립하여 CPU 양보
- 컨텍스트 스위칭 비용 고려

#### 실무 예시
- 커널 인터럽트 핸들러 (스핀락)
- 유저 스레드 동기화 (뮤텍스)

#### 면접 답변 예시
**1분**: "임계구역이 컨텍스트 스위칭보다 짧으면 스핀락, 길면 뮤텍스입니다."

**3분**: "단일 CPU에서 스핀락은 비효율적입니다. 멀티코어에서 짧은 임계구역은 스핀락이 유리합니다."

#### 꼬리 질문
- 적응형 뮤텍스란?
- 하이브리드 락?

---

### Q30. 프로세스 동기화 문제들은?

#### A. 핵심 요약
생산자-소비자, 읽기-쓰기, 식사하는 철학자 등 고전 문제가 있습니다.

#### 상세 설명
- 생산자-소비자: 버퍼 동기화
- 식사하는 철학자: 데드락 예시
- 읽기-쓰기: 동시 읽기 허용

#### 실무 예시
- 메시지 큐
- 데이터베이스 접근

#### 면접 답변 예시
**1분**: "생산자-소비자는 세마포어로, 식사하는 철학자는 자원 순서로 해결합니다."

**3분**: "실무에서는 라이브러리가 제공하는 동기화 primitive를 사용하지만, 원리 이해가 디버깅에 필수입니다."

#### 꼬리 질문
- 잠자는 이발사 문제?
- 담배 피는 사람들 문제?

---

### Q31. TLB(Translation Lookaside Buffer)란?

#### A. 핵심 요약
페이지 테이블 항목을 캐싱하여 주소 변환을 가속합니다.

#### 상세 설명
- 연관 캐시로 빠른 검색
- TLB 미스 시 페이지 테이블 참조
- 컨텍스트 스위칭 시 플러시

#### 실무 예시
- Huge Page로 TLB 효율 향상
- PCID로 플러시 회피

#### 면접 답변 예시
**1분**: "TLB는 가상→물리 주소 변환을 캐싱하여 메모리 접근을 가속합니다."

**3분**: "TLB 미스는 페이지 테이블 워크가 필요해 느립니다. Huge Page는 적은 TLB 항목으로 큰 메모리를 커버합니다."

#### 꼬리 질문
- TLB 플러시 영향?
- Huge Page 장단점?

---

### Q32. 다단계 페이지 테이블이란?

#### A. 핵심 요약
페이지 테이블을 계층적으로 구성하여 공간을 절약합니다.

#### 상세 설명
- 사용하지 않는 영역은 테이블 생성 안 함
- x86-64: 4단계 (PML4)
- 접근 시간 증가 (TLB로 완화)

#### 실무 예시
- 희소 주소 공간 효율적 표현
- 64비트 시스템

#### 면접 답변 예시
**1분**: "다단계 테이블은 사용 영역만 테이블을 만들어 메모리를 절약합니다."

**3분**: "64비트에서 단일 테이블은 불가능합니다. 4단계 워크가 필요하지만 TLB가 대부분 캐싱합니다."

#### 꼬리 질문
- Inverted Page Table이란?
- 5-level paging?

---

### Q33. Copy-on-Write(COW)란?

#### A. 핵심 요약
쓰기 발생 시점까지 복사를 지연하여 효율성을 높입니다.

#### 상세 설명
- fork() 시 페이지 공유
- 쓰기 시 페이지 폴트로 복사
- 메모리, 시간 절약

#### 실무 예시
- fork + exec 패턴
- Redis 스냅샷

#### 면접 답변 예시
**1분**: "COW는 fork 시 페이지를 공유하고, 수정 시에만 복사하여 효율적입니다."

**3분**: "대부분의 fork는 exec으로 이어져 복사가 불필요합니다. Redis는 COW로 백그라운드 저장합니다."

#### 꼬리 질문
- vfork란?
- clone()과 차이?

---

### Q34. OOM Killer란?

#### A. 핵심 요약
메모리 부족 시 프로세스를 종료하여 시스템을 보호합니다.

#### 상세 설명
- oom_score로 희생자 선정
- 조정 가능 (oom_score_adj)
- cgroup으로 그룹별 제한

#### 실무 예시
- 컨테이너 메모리 제한
- 중요 프로세스 보호

#### 면접 답변 예시
**1분**: "OOM Killer는 메모리 고갈 시 점수가 높은 프로세스를 종료합니다."

**3분**: "메모리 사용량, 실행 시간, 권한 등으로 점수를 계산합니다. 중요 서비스는 oom_score_adj를 낮게 설정합니다."

#### 꼬리 질문
- oom_score 계산 방법?
- 메모리 오버커밋이란?

---

### Q35. 프로세스 스케줄링 지표들은?

#### A. 핵심 요약
CPU 이용률, 처리량, 대기시간, 응답시간, 반환시간을 측정합니다.

#### 상세 설명
- CPU 이용률: CPU 사용 비율
- 처리량: 단위 시간당 완료 작업 수
- 응답시간: 요청~첫 응답

#### 실무 예시
- 서버 성능 모니터링
- SLA 지표

#### 면접 답변 예시
**1분**: "대화형은 응답시간, 배치는 처리량, 실시간은 데드라인 만족이 중요합니다."

**3분**: "지표들은 트레이드오프 관계입니다. 워크로드에 따라 최적화 목표가 다릅니다."

#### 꼬리 질문
- 공정성 지표?
- Jain's Fairness Index?

---

### Q36. CFS(Completely Fair Scheduler)란?

#### A. 핵심 요약
Linux의 기본 스케줄러로, 가상 실행 시간으로 공정성을 보장합니다.

#### 상세 설명
- vruntime: 가중치 반영 실행 시간
- Red-Black 트리로 관리
- nice 값으로 우선순위 조절

#### 실무 예시
- nice, renice 명령어
- cgroup CPU 할당

#### 면접 답변 예시
**1분**: "CFS는 vruntime이 가장 작은 태스크를 선택하여 공정하게 CPU를 분배합니다."

**3분**: "우선순위가 높으면 vruntime이 느리게 증가합니다. O(log n) 삽입/선택으로 효율적입니다."

#### 꼬리 질문
- vruntime 계산 방법?
- 그룹 스케줄링?

---

### Q37. 실시간 스케줄링이란?

#### A. 핵심 요약
데드라인을 보장하는 스케줄링 정책입니다.

#### 상세 설명
- 하드 실시간 vs 소프트 실시간
- SCHED_FIFO, SCHED_RR
- Rate Monotonic, EDF

#### 실무 예시
- 임베디드 시스템
- 오디오/비디오 처리

#### 면접 답변 예시
**1분**: "실시간 스케줄링은 데드라인 준수가 핵심으로, FIFO나 RR 정책을 사용합니다."

**3분**: "SCHED_DEADLINE은 EDF를 구현하며, 일반 태스크보다 항상 높은 우선순위입니다."

#### 꼬리 질문
- 하드 vs 소프트 실시간?
- Priority Inversion?

---

### Q38. Priority Inversion이란?

#### A. 핵심 요약
높은 우선순위 태스크가 낮은 우선순위에 의해 간접 블로킹되는 현상입니다.

#### 상세 설명
- 중간 우선순위가 낮은 우선순위를 선점
- Priority Inheritance로 해결
- Priority Ceiling Protocol

#### 실무 예시
- Mars Pathfinder 버그
- 실시간 시스템

#### 면접 답변 예시
**1분**: "우선순위 역전은 락을 가진 낮은 태스크가 중간에 의해 선점되어 높은 태스크가 대기합니다."

**3분**: "Priority Inheritance는 락 보유자의 우선순위를 일시적으로 높여 해결합니다."

#### 꼬리 질문
- Priority Inheritance란?
- Priority Ceiling이란?

---

### Q39. 파일 시스템 구조는?

#### A. 핵심 요약
부트 블록, 슈퍼블록, inode, 데이터 블록으로 구성됩니다.

#### 상세 설명
- 슈퍼블록: 파일 시스템 메타데이터
- inode: 파일별 메타데이터
- 디렉토리: 이름-inode 매핑

#### 실무 예시
- df, stat 명령어
- inode 고갈 문제

#### 면접 답변 예시
**1분**: "inode에 파일 메타데이터와 블록 포인터가, 디렉토리에 이름 매핑이 저장됩니다."

**3분**: "하드링크는 같은 inode를 가리키고, 심볼릭 링크는 경로를 저장합니다."

#### 꼬리 질문
- 하드링크 vs 심볼릭 링크?
- inode 구조?

---

### Q40. 저널링 파일 시스템이란?

#### A. 핵심 요약
변경 사항을 저널에 먼저 기록하여 일관성을 보장합니다.

#### 상세 설명
- Write-Ahead Logging
- 크래시 복구 가속
- 메타데이터 저널링 vs 풀 저널링

#### 실무 예시
- ext4, NTFS, XFS
- fsck 시간 단축

#### 면접 답변 예시
**1분**: "저널링은 변경을 먼저 로그에 기록하여 크래시 후 빠른 복구를 합니다."

**3분**: "메타데이터만 저널링하면 성능이 좋지만 데이터 손실 가능성이 있습니다. 풀 저널링은 안전하지만 느립니다."

#### 꼬리 질문
- COW 파일 시스템과 비교?
- 체크섬 기반 무결성?

---

### Q41. 버퍼 캐시와 페이지 캐시란?

#### A. 핵심 요약
디스크 I/O를 메모리에 캐싱하여 성능을 높입니다.

#### 상세 설명
- 페이지 캐시: 파일 데이터
- 버퍼 캐시: 블록 장치 (현재 통합됨)
- Write-back vs Write-through

#### 실무 예시
- sync, fsync
- drop_caches

#### 면접 답변 예시
**1분**: "페이지 캐시는 파일 내용을 메모리에 유지하여 반복 읽기를 가속합니다."

**3분**: "write()는 캐시에 쓰고 반환합니다. fsync()로 디스크 동기화를 보장합니다."

#### 꼬리 질문
- fsync vs fdatasync?
- O_DIRECT란?

---

### Q42. RAID 레벨들은?

#### A. 핵심 요약
디스크를 조합하여 성능, 용량, 신뢰성을 최적화합니다.

#### 상세 설명
- RAID 0: 스트라이핑, 성능
- RAID 1: 미러링, 신뢰성
- RAID 5/6: 패리티, 균형

#### 실무 예시
- 서버 스토리지 구성
- 소프트웨어 RAID

#### 면접 답변 예시
**1분**: "RAID 5는 분산 패리티로 1개 디스크 장애를, RAID 6은 2개를 허용합니다."

**3분**: "RAID 10은 미러 + 스트라이프로 성능과 신뢰성을 모두 제공합니다."

#### 꼬리 질문
- RAID 5 vs RAID 10?
- Write Hole 문제?

---

### Q43. 디스크 스케줄링 알고리즘은?

#### A. 핵심 요약
디스크 헤드 이동을 최소화하여 I/O 성능을 높입니다.

#### 상세 설명
- FCFS, SSTF, SCAN, C-SCAN
- 최근: CFQ, Deadline, BFQ, mq-deadline

#### 실무 예시
- SSD에서 스케줄링 영향 감소
- I/O 스케줄러 변경

#### 면접 답변 예시
**1분**: "SCAN은 엘리베이터처럼 한 방향으로 이동하며 요청을 처리합니다."

**3분**: "SSD는 탐색 시간이 없어 단순한 스케줄러도 효율적입니다. NVMe는 멀티큐로 병렬 처리합니다."

#### 꼬리 질문
- HDD vs SSD 스케줄링 차이?
- mq-deadline이란?

---

### Q44. 가상화란?

#### A. 핵심 요약
물리 자원을 추상화하여 여러 가상 환경을 제공합니다.

#### 상세 설명
- 하이퍼바이저: Type 1(베어메탈), Type 2(호스팅)
- 전가상화 vs 반가상화
- 하드웨어 지원 (VT-x, AMD-V)

#### 실무 예시
- VMware, KVM, VirtualBox
- 클라우드 인프라

#### 면접 답변 예시
**1분**: "가상화는 하이퍼바이저로 물리 자원을 분할하여 여러 OS를 실행합니다."

**3분**: "Type 1은 성능이 좋고, Type 2는 설치가 쉽습니다. 하드웨어 가상화 지원으로 오버헤드가 줄었습니다."

#### 꼬리 질문
- KVM 동작 원리?
- 전가상화 vs 반가상화?

---

### Q45. 컨테이너와 가상머신 차이는?

#### A. 핵심 요약
컨테이너는 커널 공유로 가볍고, VM은 완전 격리로 안전합니다.

#### 상세 설명
- 컨테이너: 네임스페이스, cgroup
- VM: 전체 OS 가상화
- 부팅 시간, 자원 사용량 차이

#### 실무 예시
- Docker, Kubernetes
- 마이크로서비스

#### 면접 답변 예시
**1분**: "컨테이너는 커널을 공유하여 가볍고 빠르지만, 격리 수준은 VM보다 낮습니다."

**3분**: "네임스페이스로 프로세스, 네트워크 등을 격리하고, cgroup으로 자원을 제한합니다."

#### 꼬리 질문
- 네임스페이스 종류?
- cgroup v1 vs v2?

---

### Q46. 네임스페이스란?

#### A. 핵심 요약
프로세스에 독립된 시스템 뷰를 제공하는 커널 기능입니다.

#### 상세 설명
- PID, Network, Mount, User 등
- 컨테이너 격리의 기반
- unshare, clone으로 생성

#### 실무 예시
- Docker 격리
- 루트리스 컨테이너

#### 면접 답변 예시
**1분**: "네임스페이스는 PID, 네트워크 등을 격리하여 컨테이너처럼 독립 환경을 만듭니다."

**3분**: "User 네임스페이스로 비루트 사용자가 컨테이너 내 루트처럼 동작할 수 있습니다."

#### 꼬리 질문
- 7가지 네임스페이스?
- User 네임스페이스 보안?

---

### Q47. cgroup이란?

#### A. 핵심 요약
프로세스 그룹의 자원 사용을 제한하고 모니터링합니다.

#### 상세 설명
- CPU, 메모리, I/O, 네트워크 제한
- 계층적 구조
- cgroup v2로 통합

#### 실무 예시
- 컨테이너 리소스 제한
- systemd 서비스 제한

#### 면접 답변 예시
**1분**: "cgroup은 프로세스 그룹의 CPU, 메모리 등을 제한하여 자원 격리를 합니다."

**3분**: "v2는 단일 계층으로 간소화되었고, PSI(Pressure Stall Information)을 제공합니다."

#### 꼬리 질문
- cgroup v1 vs v2?
- PSI란?

---

### Q48. seccomp란?

#### A. 핵심 요약
프로세스가 호출 가능한 시스템 콜을 제한하는 보안 기능입니다.

#### 상세 설명
- 화이트리스트/블랙리스트 모드
- BPF 필터로 정교한 규칙
- 컨테이너 보안 강화

#### 실무 예시
- Docker 기본 seccomp 프로파일
- 샌드박스

#### 면접 답변 예시
**1분**: "seccomp은 허용된 시스템 콜만 호출하게 하여 공격 표면을 줄입니다."

**3분**: "seccomp-bpf로 인자까지 검사할 수 있습니다. 컨테이너에서 위험한 시스템 콜을 차단합니다."

#### 꼬리 질문
- seccomp 모드 종류?
- AppArmor, SELinux와 조합?

---

### Q49. 메모리 매핑(mmap)이란?

#### A. 핵심 요약
파일이나 장치를 프로세스 주소 공간에 매핑합니다.

#### 상세 설명
- 파일 I/O를 메모리 접근으로
- 공유 메모리 IPC
- 익명 매핑

#### 실무 예시
- 대용량 파일 처리
- 공유 라이브러리 로딩

#### 면접 답변 예시
**1분**: "mmap은 파일을 메모리처럼 접근하게 하여 효율적인 I/O를 제공합니다."

**3분**: "read/write보다 복사가 줄어듭니다. 페이지 폴트로 필요한 부분만 로드됩니다."

#### 꼬리 질문
- MAP_SHARED vs MAP_PRIVATE?
- munmap 타이밍?

---

### Q50. 시그널이란?

#### A. 핵심 요약
프로세스에 비동기 이벤트를 알리는 소프트웨어 인터럽트입니다.

#### 상세 설명
- 표준 시그널: SIGKILL, SIGTERM, SIGINT
- 시그널 핸들러 등록
- 마스킹, 펜딩

#### 실무 예시
- Ctrl+C (SIGINT)
- 데몬 설정 리로드 (SIGHUP)

#### 면접 답변 예시
**1분**: "시그널은 프로세스 간 비동기 통신으로, 핸들러를 등록하여 처리합니다."

**3분**: "SIGKILL, SIGSTOP은 핸들링 불가합니다. 시그널 안전 함수만 핸들러에서 사용해야 합니다."

#### 꼬리 질문
- 시그널 안전 함수란?
- sigaction vs signal?

---

### Q51. epoll이란?

#### A. 핵심 요약
Linux의 효율적인 I/O 이벤트 알림 인터페이스입니다.

#### 상세 설명
- Edge-triggered vs Level-triggered
- O(1) 이벤트 확인
- select/poll 대체

#### 실무 예시
- Nginx, Redis
- 고성능 네트워크 서버

#### 면접 답변 예시
**1분**: "epoll은 관심 fd를 등록하고 이벤트 발생 시에만 알림받아 효율적입니다."

**3분**: "ET 모드는 상태 변화 시에만 알림하여 효율적이지만 완전 처리가 필요합니다."

#### 꼬리 질문
- ET vs LT?
- io_uring과 비교?

---

### Q52. io_uring이란?

#### A. 핵심 요약
Linux의 새로운 비동기 I/O 인터페이스로, 시스템 콜 오버헤드를 줄입니다.

#### 상세 설명
- 링 버퍼로 제출/완료 큐
- 커널/유저 공간 공유 메모리
- 배치 처리

#### 실무 예시
- 고성능 스토리지
- 네트워크 서버

#### 면접 답변 예시
**1분**: "io_uring은 시스템 콜 없이 I/O를 요청하여 고성능을 달성합니다."

**3분**: "SQ와 CQ를 mmap으로 공유하여 동기화 없이 통신합니다. Polling 모드로 인터럽트도 제거 가능합니다."

#### 꼬리 질문
- SQ/CQ 동작 방식?
- liburing?

---

### Q53. 프로세스 우선순위 관리는?

#### A. 핵심 요약
nice, ionice, CPU affinity로 프로세스 자원 할당을 조절합니다.

#### 상세 설명
- nice: CPU 스케줄링 우선순위
- ionice: I/O 스케줄링 우선순위
- taskset: CPU 코어 바인딩

#### 실무 예시
- 백그라운드 작업 우선순위 낮춤
- NUMA 최적화

#### 면접 답변 예시
**1분**: "nice -19~20으로 CPU, ionice로 I/O 우선순위를 조절합니다."

**3분**: "cgroup으로 더 정교하게 제어합니다. CPU affinity는 캐시 효율을 높입니다."

#### 꼬리 질문
- NUMA 인지 스케줄링?
- isolcpus?

---

### Q54. 리눅스 커널 모듈이란?

#### A. 핵심 요약
실행 중 커널에 동적으로 로드/언로드 가능한 코드입니다.

#### 상세 설명
- 디바이스 드라이버
- insmod, rmmod, modprobe
- 커널 심볼 익스포트

#### 실무 예시
- 네트워크 드라이버
- 파일시스템 드라이버

#### 면접 답변 예시
**1분**: "커널 모듈은 재부팅 없이 커널 기능을 확장합니다."

**3분**: "모듈은 커널 주소 공간에서 실행되어 버그가 시스템 전체에 영향을 줄 수 있습니다."

#### 꼬리 질문
- 모듈 서명?
- DKMS란?

---

### Q55. 시스템 콜 오버헤드란?

#### A. 핵심 요약
유저-커널 모드 전환 비용으로, 수백 나노초가 소요됩니다.

#### 상세 설명
- 컨텍스트 저장/복원
- 캐시, TLB 영향
- vDSO, io_uring으로 최소화

#### 실무 예시
- gettimeofday vDSO
- 배칭으로 시스템 콜 줄이기

#### 면접 답변 예시
**1분**: "시스템 콜은 모드 전환 비용이 있어 빈번하면 성능에 영향을 줍니다."

**3분**: "vDSO는 간단한 시스템 콜을 유저 공간에서 처리합니다. 배칭으로 호출 횟수를 줄입니다."

#### 꼬리 질문
- vDSO 동작 원리?
- syscall vs sysenter?

---

## Level 3 - 고급 (5~7년차) | 25문제

---

### Q56. 메모리 배리어란?

#### A. 핵심 요약
CPU와 컴파일러의 명령어 재정렬을 방지하여 메모리 일관성을 보장합니다.

#### 상세 설명
- 컴파일러 배리어 vs 하드웨어 배리어
- 읽기, 쓰기, 전체 배리어
- volatile만으로는 부족

#### 실무 예시
- 락 프리 알고리즘
- 멀티코어 동기화

#### 면접 답변 예시
**1분**: "메모리 배리어는 특정 지점 이전의 메모리 연산이 완료됨을 보장합니다."

**3분**: "x86은 강한 메모리 모델이지만, ARM은 약해서 명시적 배리어가 필요합니다. acquire-release 시멘틱이 일반적입니다."

#### 꼬리 질문
- acquire vs release?
- 메모리 모델이란?

---

### Q57. RCU(Read-Copy-Update)란?

#### A. 핵심 요약
읽기는 락 없이, 쓰기는 복사 후 교체하는 동기화 기법입니다.

#### 상세 설명
- 읽기: 오버헤드 없음
- 쓰기: 복사 → 수정 → 포인터 교체
- Grace Period 후 구버전 삭제

#### 실무 예시
- Linux 커널 (라우팅 테이블)
- 읽기 많은 자료구조

#### 면접 답변 예시
**1분**: "RCU는 읽기 경로에 동기화가 없어 읽기 집약 워크로드에 최적입니다."

**3분**: "읽기 중인 스레드가 없을 때(Grace Period)까지 기다린 후 구 데이터를 삭제합니다."

#### 꼬리 질문
- Grace Period 감지 방법?
- SRCU란?

---

### Q58. 락 프리 vs 대기 프리란?

#### A. 핵심 요약
락 프리는 최소 하나의 진행, 대기 프리는 모든 스레드의 진행을 보장합니다.

#### 상세 설명
- 락 프리: 전역 진행 보장
- 대기 프리: 개별 진행 보장
- CAS 기반 구현

#### 실무 예시
- 락 프리 큐, 스택
- Java ConcurrentLinkedQueue

#### 면접 답변 예시
**1분**: "락 프리는 기아 가능, 대기 프리는 유한 시간 내 완료를 보장합니다."

**3분**: "대기 프리는 구현이 어렵고 성능이 낮을 수 있습니다. 실무에서는 락 프리가 많습니다."

#### 꼬리 질문
- Obstruction-free란?
- ABA 문제 해결법?

---

### Q59. NUMA(Non-Uniform Memory Access)란?

#### A. 핵심 요약
메모리 접근 시간이 위치에 따라 다른 아키텍처입니다.

#### 상세 설명
- 로컬 메모리 vs 원격 메모리
- NUMA 노드, 거리
- numactl로 제어

#### 실무 예시
- 대규모 서버
- 데이터베이스 최적화

#### 면접 답변 예시
**1분**: "NUMA에서 로컬 메모리 접근이 원격보다 빨라 데이터 배치가 중요합니다."

**3분**: "프로세스를 NUMA 노드에 바인딩하고 메모리를 로컬 할당하면 성능이 향상됩니다."

#### 꼬리 질문
- NUMA 인지 메모리 할당?
- 인터리빙 정책?

---

### Q60. 페이지 컬러링이란?

#### A. 핵심 요약
캐시 충돌을 줄이기 위해 페이지를 캐시 위치에 맞게 할당합니다.

#### 상세 설명
- 캐시 인덱스와 페이지 오프셋 관계
- 같은 색의 페이지는 같은 캐시 세트
- 분산 할당으로 충돌 감소

#### 실무 예시
- FreeBSD 페이지 컬러링
- 실시간 시스템

#### 면접 답변 예시
**1분**: "페이지 컬러링은 가상 페이지를 캐시에 균등 분산하여 충돌을 줄입니다."

**3분**: "현대 CPU의 큰 캐시와 높은 연관도로 중요성이 줄었지만, 특수 환경에서 여전히 유효합니다."

#### 꼬리 질문
- 캐시 연관도란?
- Huge Page와 캐시?

---

### Q61. Transparent Huge Pages란?

#### A. 핵심 요약
커널이 자동으로 대용량 페이지를 관리합니다.

#### 상세 설명
- 4KB → 2MB/1GB 페이지
- khugepaged 데몬
- TLB 효율 향상

#### 실무 예시
- 데이터베이스, JVM
- 때로는 지연 유발로 비활성화

#### 면접 답변 예시
**1분**: "THP는 자동으로 큰 페이지를 사용하여 TLB 미스를 줄입니다."

**3분**: "페이지 압축/분할 시 지연이 발생할 수 있어 Redis 등에서는 비활성화를 권장합니다."

#### 꼬리 질문
- THP 단점?
- HugeTLB vs THP?

---

### Q62. eBPF란?

#### A. 핵심 요약
커널 내에서 안전하게 실행되는 샌드박스 프로그램입니다.

#### 상세 설명
- 트레이싱, 네트워킹, 보안
- 검증기로 안전성 보장
- JIT 컴파일

#### 실무 예시
- BCC, bpftrace
- Cilium (네트워킹)

#### 면접 답변 예시
**1분**: "eBPF는 커널 수정 없이 커널 레벨 기능을 추가하는 안전한 방법입니다."

**3분**: "트레이싱(성능 분석), 네트워킹(XDP), 보안(Seccomp) 등 다양하게 활용됩니다."

#### 꼬리 질문
- eBPF 검증기 역할?
- XDP란?

---

### Q63. 리눅스 보안 모듈이란?

#### A. 핵심 요약
커널 훅으로 접근 제어를 강화하는 프레임워크입니다.

#### 상세 설명
- SELinux, AppArmor
- MAC(Mandatory Access Control)
- 정책 기반 제어

#### 실무 예시
- 컨테이너 보안
- 서버 하드닝

#### 면접 답변 예시
**1분**: "LSM은 DAC 외에 MAC을 추가하여 더 세밀한 접근 제어를 합니다."

**3분**: "SELinux는 라벨 기반, AppArmor는 경로 기반 정책입니다. 컨테이너에서 중요한 보안 계층입니다."

#### 꼬리 질문
- SELinux vs AppArmor?
- DAC vs MAC?

---

### Q64. 커널 동기화 primitive들은?

#### A. 핵심 요약
spinlock, mutex, semaphore, rwlock, RCU 등을 상황에 맞게 사용합니다.

#### 상세 설명
- 인터럽트 컨텍스트: spinlock
- 프로세스 컨텍스트: mutex/semaphore
- 읽기 집약: rwlock/RCU

#### 실무 예시
- 드라이버 개발
- 커널 모듈

#### 면접 답변 예시
**1분**: "인터럽트에서는 슬립 불가하므로 spinlock, 프로세스에서는 mutex를 사용합니다."

**3분**: "spinlock은 선점을 비활성화합니다. PREEMPT_RT에서는 spinlock도 슬립 가능합니다."

#### 꼬리 질문
- raw_spinlock vs spinlock?
- PREEMPT_RT란?

---

### Q65. 커널 메모리 할당자들은?

#### A. 핵심 요약
버디 시스템, SLAB/SLUB/SLOB으로 커널 메모리를 관리합니다.

#### 상세 설명
- 버디: 페이지 단위
- SLUB: 객체 캐싱 (현재 기본)
- kmalloc, vmalloc

#### 실무 예시
- 드라이버 메모리 할당
- /proc/slabinfo

#### 면접 답변 예시
**1분**: "SLUB은 같은 크기 객체를 캐싱하여 할당/해제를 빠르게 합니다."

**3분**: "vmalloc은 연속 가상 주소지만 물리적으로는 불연속입니다. kmalloc은 물리적 연속입니다."

#### 꼬리 질문
- kmalloc vs vmalloc?
- 메모리 풀?

---

### Q66. 페이지 폴트 처리 과정은?

#### A. 핵심 요약
유효하지 않은 페이지 접근 시 핸들러가 페이지를 로드하거나 오류 처리합니다.

#### 상세 설명
- Minor: 메모리에 있지만 매핑 안 됨
- Major: 디스크에서 로드 필요
- Segfault: 잘못된 접근

#### 실무 예시
- 요구 페이징
- COW 처리

#### 면접 답변 예시
**1분**: "Minor 폴트는 매핑만, Major 폴트는 디스크 I/O가 필요합니다."

**3분**: "페이지 폴트 핸들러가 VMA를 확인하고 적절한 동작(할당, 로드, 시그널)을 수행합니다."

#### 꼬리 질문
- VMA란?
- userfaultfd?

---

### Q67. 프로세스 주소 공간 레이아웃은?

#### A. 핵심 요약
텍스트, 데이터, BSS, 힙, 스택, mmap 영역으로 구성됩니다.

#### 상세 설명
- 텍스트: 코드 (읽기 전용)
- 힙: 위로 성장
- 스택: 아래로 성장

#### 실무 예시
- pmap 명령어
- ASLR 보안

#### 면접 답변 예시
**1분**: "힙은 낮은 주소에서 위로, 스택은 높은 주소에서 아래로 성장합니다."

**3분**: "ASLR은 각 영역의 시작 주소를 무작위화하여 공격을 어렵게 합니다."

#### 꼬리 질문
- ASLR이란?
- PIE란?

---

### Q68. OOM 상황 디버깅은?

#### A. 핵심 요약
메모리 누수, 과다 사용을 분석하여 OOM 원인을 찾습니다.

#### 상세 설명
- dmesg OOM 로그 분석
- /proc/meminfo, smaps
- valgrind, AddressSanitizer

#### 실무 예시
- 컨테이너 OOM 분석
- 메모리 누수 탐지

#### 면접 답변 예시
**1분**: "OOM 로그에서 각 프로세스의 RSS와 oom_score를 확인합니다."

**3분**: "smaps로 프로세스별 메모리 상세를 분석하고, cgroup 메모리 제한도 확인합니다."

#### 꼬리 질문
- RSS vs VSS?
- 메모리 누수 탐지 도구?

---

### Q69. 성능 분석 도구들은?

#### A. 핵심 요약
perf, strace, top, vmstat 등으로 시스템 성능을 분석합니다.

#### 상세 설명
- perf: CPU 프로파일링
- strace: 시스템 콜 추적
- vmstat, iostat: 시스템 통계

#### 실무 예시
- CPU 병목 분석
- I/O 지연 분석

#### 면접 답변 예시
**1분**: "perf로 CPU, iostat으로 디스크, vmstat으로 전체 시스템을 분석합니다."

**3분**: "Brendan Gregg의 USE 방법론으로 Utilization, Saturation, Errors를 체계적으로 확인합니다."

#### 꼬리 질문
- USE 방법론?
- Flame Graph란?

---

### Q70. ftrace와 perf 차이는?

#### A. 핵심 요약
ftrace는 커널 내장 트레이서, perf는 PMU 기반 프로파일러입니다.

#### 상세 설명
- ftrace: 함수 트레이싱
- perf: 샘플링 프로파일링
- 둘 다 eBPF로 확장 가능

#### 실무 예시
- 함수 호출 추적 (ftrace)
- 핫스팟 분석 (perf)

#### 면접 답변 예시
**1분**: "ftrace는 함수 진입/퇴장을 정확히, perf는 통계적 샘플링을 합니다."

**3분**: "perf는 하드웨어 PMU로 캐시 미스, 분기 예측 실패 등도 측정합니다."

#### 꼬리 질문
- PMU란?
- kprobes/uprobes?

---

### Q71. 리얼타임 리눅스란?

#### A. 핵심 요약
PREEMPT_RT 패치로 결정적 지연 시간을 제공합니다.

#### 상세 설명
- 완전 선점형 커널
- 스핀락을 RT 뮤텍스로
- 우선순위 상속 지원

#### 실무 예시
- 산업 제어
- 오디오/비디오

#### 면접 답변 예시
**1분**: "PREEMPT_RT는 커널의 대부분을 선점 가능하게 하여 지연을 줄입니다."

**3분**: "인터럽트 핸들러도 스레드화되어 우선순위로 스케줄됩니다. cyclictest로 지연을 측정합니다."

#### 꼬리 질문
- 소프트 vs 하드 실시간?
- cyclictest 결과 해석?

---

### Q72. 워크큐와 태스크릿 차이는?

#### A. 핵심 요약
워크큐는 프로세스 컨텍스트, 태스크릿은 소프트웨어 인터럽트 컨텍스트입니다.

#### 상세 설명
- 태스크릿: 빠르지만 슬립 불가
- 워크큐: 슬립 가능, 유연
- Threaded IRQ도 대안

#### 실무 예시
- 네트워크 드라이버
- Bottom Half 처리

#### 면접 답변 예시
**1분**: "워크큐는 슬립이 필요한 지연 작업에, 태스크릿은 빠른 처리에 사용합니다."

**3분**: "현대 커널은 태스크릿보다 워크큐나 Threaded IRQ를 권장합니다."

#### 꼬리 질문
- Threaded IRQ란?
- softirq vs tasklet?

---

### Q73. 파일 시스템 캐시 정책은?

#### A. 핵심 요약
write-back이 기본이며, sync 옵션으로 안전성을 높입니다.

#### 상세 설명
- write-back: 지연 쓰기, 빠름
- write-through: 즉시 쓰기
- dirty ratio로 플러시 조절

#### 실무 예시
- 데이터베이스 fsync
- 배터리 시스템

#### 면접 답변 예시
**1분**: "write-back은 캐시에 쓰고 나중에 디스크에 플러시하여 성능이 좋습니다."

**3분**: "dirty_ratio 초과 시 동기 플러시가 발생하여 지연이 생깁니다. 적절한 튜닝이 필요합니다."

#### 꼬리 질문
- dirty_ratio vs dirty_background_ratio?
- 데이터 무결성 보장?

---

### Q74. Zero-Copy란?

#### A. 핵심 요약
사용자 공간 복사 없이 데이터를 전송하여 효율을 높입니다.

#### 상세 설명
- sendfile, splice
- DMA로 직접 전송
- 메모리 대역폭 절약

#### 실무 예시
- 웹 서버 정적 파일
- 네트워크 파일 전송

#### 면접 답변 예시
**1분**: "Zero-copy는 커널-유저 공간 복사를 제거하여 CPU와 메모리를 절약합니다."

**3분**: "sendfile은 파일→소켓, splice는 파이프 기반으로 zero-copy를 구현합니다."

#### 꼬리 질문
- sendfile vs splice?
- DPDK와 zero-copy?

---

### Q75. 커널 우회 기술들은?

#### A. 핵심 요약
DPDK, SPDK, XDP로 커널을 우회하여 고성능을 달성합니다.

#### 상세 설명
- 사용자 공간 드라이버
- 폴링 기반 처리
- 시스템 콜 오버헤드 제거

#### 실무 예시
- 고성능 네트워킹
- NVMe 스토리지

#### 면접 답변 예시
**1분**: "DPDK는 네트워크 패킷을 커널 우회로 처리하여 수백만 PPS를 달성합니다."

**3분**: "전용 코어와 Huge Page를 사용합니다. 커널 보호 기능을 포기하는 트레이드오프가 있습니다."

#### 꼬리 질문
- XDP vs DPDK?
- AF_XDP란?

---

### Q76. 메모리 압축이란?

#### A. 핵심 요약
스왑 대신 메모리 내 압축으로 공간을 확보합니다.

#### 상세 설명
- zswap: 스왑 페이지 압축 캐시
- zram: 압축 RAM 디스크
- LZ4, ZSTD 압축

#### 실무 예시
- 임베디드 시스템
- 클라우드 메모리 최적화

#### 면접 답변 예시
**1분**: "zswap은 스왑 전 페이지를 압축하여 I/O를 줄입니다."

**3분**: "압축률에 따라 2-3배 메모리 효과가 있지만 CPU 오버헤드가 있습니다."

#### 꼬리 질문
- zswap vs zram?
- KSM이란?

---

### Q77. 커널 라이브 패칭이란?

#### A. 핵심 요약
재부팅 없이 실행 중인 커널에 패치를 적용합니다.

#### 상세 설명
- kpatch, livepatch
- 함수 교체 방식
- 보안 패치에 유용

#### 실무 예시
- 무중단 보안 업데이트
- 클라우드 인프라

#### 면접 답변 예시
**1분**: "라이브 패치는 함수 진입점을 새 함수로 리다이렉트합니다."

**3분**: "복잡한 패치는 불가능하고, 결국 재부팅이 필요합니다. 긴급 보안 패치에 유용합니다."

#### 꼬리 질문
- kpatch 동작 원리?
- 제약 사항?

---

### Q78. PSI(Pressure Stall Information)란?

#### A. 핵심 요약
CPU, 메모리, I/O 자원 부족을 정량화합니다.

#### 상세 설명
- some: 일부 태스크 지연
- full: 모든 태스크 지연
- 10초/60초/300초 평균

#### 실무 예시
- cgroup 자원 모니터링
- 자동 스케일링 트리거

#### 면접 답변 예시
**1분**: "PSI는 자원 경쟁으로 인한 지연을 백분율로 보여줍니다."

**3분**: "oomd는 PSI를 기반으로 메모리 부족을 감지하고 선제적으로 프로세스를 종료합니다."

#### 꼬리 질문
- PSI 메트릭 해석?
- oomd란?

---

### Q79. cgroup v2의 변화는?

#### A. 핵심 요약
단일 계층, 통합 컨트롤러로 단순화되었습니다.

#### 상세 설명
- 단일 계층 구조
- PSI 지원
- threaded 모드

#### 실무 예시
- systemd 기본 사용
- Kubernetes 지원

#### 면접 답변 예시
**1분**: "v2는 하나의 계층에 모든 컨트롤러를 배치하여 일관성을 높였습니다."

**3분**: "스레드 레벨 제어와 PSI로 더 정교한 자원 관리가 가능합니다."

#### 꼬리 질문
- v1과 공존 방법?
- freezer cgroup?

---

### Q80. 시스템 안정성 확보 방법은?

#### A. 핵심 요약
Watchdog, kdump, 모니터링으로 장애를 감지하고 복구합니다.

#### 상세 설명
- Watchdog: 시스템 행 감지
- kdump: 크래시 덤프 수집
- systemd 서비스 재시작

#### 실무 예시
- 서버 무중단 운영
- 장애 분석

#### 면접 답변 예시
**1분**: "Watchdog이 행을 감지하면 재부팅하고, kdump로 원인을 분석합니다."

**3분**: "systemd의 Restart=always로 서비스 자동 복구하고, 클러스터 페일오버도 구성합니다."

#### 꼬리 질문
- kdump 설정?
- 커널 패닉 분석?

---

## Level 4 - 실무 & 아키텍처 (8~10년차) | 20문제

---

### Q81. 대규모 서버 메모리 관리 전략은?

#### A. 핵심 요약
NUMA, Huge Page, cgroup으로 TB급 메모리를 효율적으로 관리합니다.

#### 상세 설명
- NUMA 인지 메모리 할당
- Huge Page로 TLB 효율화
- 메모리 대역폭 최적화

#### 실무 예시
- 데이터베이스 서버
- In-memory 캐시

#### 면접 답변 예시
**1분**: "NUMA 로컬 할당과 Huge Page로 메모리 접근 효율을 최적화합니다."

**3분**: "메모리 대역폭이 병목이 되면 채널 분산, 인터리빙을 고려합니다. cgroup으로 테넌트별 제한합니다."

#### 꼬리 질문
- 메모리 대역폭 측정?
- 원격 메모리 기술?

---

### Q82. 고성능 I/O 시스템 설계는?

#### A. 핵심 요약
io_uring, SPDK, 비동기 I/O로 고처리량, 저지연을 달성합니다.

#### 상세 설명
- Direct I/O로 캐시 우회
- NVMe 멀티큐 활용
- 폴링 모드

#### 실무 예시
- 분산 스토리지
- 실시간 로깅

#### 면접 답변 예시
**1분**: "io_uring의 SQ/CQ와 NVMe 멀티큐로 수백만 IOPS를 달성합니다."

**3분**: "커널 우회(SPDK)로 더 높은 성능이 가능하지만 복잡성이 증가합니다."

#### 꼬리 질문
- Direct I/O 주의점?
- NVMe 최적화?

---

### Q83. 컨테이너 런타임 구조는?

#### A. 핵심 요약
containerd, runc가 OCI 표준으로 컨테이너를 관리합니다.

#### 상세 설명
- High-level: containerd, CRI-O
- Low-level: runc
- OCI 이미지, 런타임 스펙

#### 실무 예시
- Kubernetes CRI
- 보안 런타임 (gVisor, Kata)

#### 면접 답변 예시
**1분**: "containerd가 이미지를 관리하고, runc가 실제 컨테이너를 생성합니다."

**3분**: "gVisor는 시스템 콜을 가상화하고, Kata는 경량 VM으로 격리를 강화합니다."

#### 꼬리 질문
- OCI 스펙?
- rootless 컨테이너?

---

### Q84. 대규모 프로세스 관리 전략은?

#### A. 핵심 요약
systemd, 프로세스 그룹, 모니터링으로 수천 프로세스를 관리합니다.

#### 상세 설명
- systemd 서비스 관리
- cgroup 자원 제한
- 프로세스 트리 구조

#### 실무 예시
- 마이크로서비스 운영
- 컨테이너 오케스트레이션

#### 면접 답변 예시
**1분**: "systemd로 의존성 관리, cgroup으로 자원 격리, 모니터링으로 상태 추적합니다."

**3분**: "PID 고갈 방지, 좀비 프로세스 정리, OOM 정책 설정이 중요합니다."

#### 꼬리 질문
- PID 네임스페이스?
- 프로세스 수 제한?

---

### Q85. 커널 튜닝 방법론은?

#### A. 핵심 요약
sysctl, 부트 파라미터로 워크로드에 맞게 커널을 최적화합니다.

#### 상세 설명
- 네트워크: 버퍼, 백로그
- 메모리: vm.* 파라미터
- 스케줄러: kernel.sched_*

#### 실무 예시
- 웹 서버 최적화
- 데이터베이스 튜닝

#### 면접 답변 예시
**1분**: "sysctl로 런타임, grub으로 부트 시 커널 파라미터를 조정합니다."

**3분**: "워크로드 특성을 분석하고 벤치마킹으로 효과를 검증합니다. 과도한 튜닝은 역효과입니다."

#### 꼬리 질문
- 중요 sysctl 파라미터?
- 튜닝 검증 방법?

---

### Q86. 시스템 보안 강화는?

#### A. 핵심 요약
LSM, seccomp, 네임스페이스, 암호화로 다계층 방어합니다.

#### 상세 설명
- SELinux/AppArmor 정책
- seccomp 시스템 콜 필터
- 커널 하드닝

#### 실무 예시
- 컨테이너 보안
- PCI-DSS 준수

#### 면접 답변 예시
**1분**: "다계층 방어로 LSM, seccomp, 네임스페이스를 조합합니다."

**3분**: "최소 권한 원칙으로 필요한 것만 허용하고, 감사 로그로 모니터링합니다."

#### 꼬리 질문
- Capabilities란?
- 보안 감사 도구?

---

### Q87. 분산 시스템의 OS 고려사항은?

#### A. 핵심 요약
시간 동기화, 네트워크 최적화, 장애 격리가 핵심입니다.

#### 상세 설명
- NTP/PTP 시간 동기화
- TCP 튜닝
- 장애 도메인 설계

#### 실무 예시
- 분산 데이터베이스
- 마이크로서비스

#### 면접 답변 예시
**1분**: "시간 동기화, 네트워크 안정성, 자원 격리가 분산 시스템의 기반입니다."

**3분**: "파티션 상황에서도 동작하도록 타임아웃, 재시도, 서킷 브레이커를 설정합니다."

#### 꼬리 질문
- 시계 드리프트 영향?
- 네트워크 파티션 대응?

---

### Q88. 하이브리드 클라우드 OS 전략은?

#### A. 핵심 요약
일관된 런타임, 이식성, 보안 정책으로 멀티 환경을 관리합니다.

#### 상세 설명
- 컨테이너로 이식성 확보
- Ansible, Terraform 자동화
- 통합 모니터링

#### 실무 예시
- 온프레미스 + 클라우드
- 멀티클라우드

#### 면접 답변 예시
**1분**: "컨테이너와 IaC로 환경 차이를 추상화하고 일관성을 유지합니다."

**3분**: "보안 정책, 네트워크 구성, 모니터링을 통합하여 운영 복잡성을 줄입니다."

#### 꼬리 질문
- 불변 인프라란?
- GitOps?

---

### Q89. 임베디드 리눅스 최적화는?

#### A. 핵심 요약
커널 최소화, 부팅 최적화, 메모리 효율화로 제약 환경에 맞춥니다.

#### 상세 설명
- 커널 모듈 최소화
- initramfs 최적화
- 읽기 전용 루트

#### 실무 예시
- IoT 디바이스
- 차량용 시스템

#### 면접 답변 예시
**1분**: "사용하지 않는 기능을 제거하고, 부팅 속도와 메모리를 최적화합니다."

**3분**: "Yocto로 맞춤 배포판을 만들고, systemd 대신 가벼운 init을 사용합니다."

#### 꼬리 질문
- Yocto vs Buildroot?
- 부팅 시간 측정?

---

### Q90. 장애 분석 방법론은?

#### A. 핵심 요약
로그, 덤프, 트레이싱으로 근본 원인을 체계적으로 분석합니다.

#### 상세 설명
- dmesg, journalctl 로그
- crash 덤프 분석
- perf, bpftrace 트레이싱

#### 실무 예시
- 커널 패닉 분석
- 성능 저하 원인 파악

#### 면접 답변 예시
**1분**: "로그로 타임라인 파악, 덤프로 상태 분석, 트레이싱으로 동작 추적합니다."

**3분**: "재현 가능하면 트레이싱으로, 크래시면 덤프로 분석합니다. 5 Whys로 근본 원인을 찾습니다."

#### 꼬리 질문
- crash 도구 사용법?
- 포스트모템 문화?

---

### Q91. 커널 디버깅 기법은?

#### A. 핵심 요약
printk, kgdb, ftrace, crash로 커널 문제를 디버깅합니다.

#### 상세 설명
- printk 레벨 조절
- KGDB 원격 디버깅
- crash로 덤프 분석

#### 실무 예시
- 드라이버 개발
- 커널 버그 수정

#### 면접 답변 예시
**1분**: "ftrace로 흐름 추적, KGDB로 브레이크포인트, crash로 사후 분석합니다."

**3분**: "KASAN, UBSAN 같은 새니타이저로 메모리 오류를 탐지합니다."

#### 꼬리 질문
- KASAN이란?
- 심볼 정보 유지?

---

### Q92. 대규모 로그 시스템 설계는?

#### A. 핵심 요약
구조화 로깅, 중앙 집중, 샘플링으로 TB급 로그를 관리합니다.

#### 상세 설명
- journald, syslog-ng
- Elasticsearch, Loki
- 로그 레벨, 로테이션

#### 실무 예시
- 분산 시스템 디버깅
- 보안 감사

#### 면접 답변 예시
**1분**: "구조화 로깅, 중앙 수집, 효율적 검색으로 대규모 로그를 관리합니다."

**3분**: "샘플링과 집계로 비용을 줄이고, 중요 로그는 별도 경로로 보존합니다."

#### 꼬리 질문
- 로그 샘플링 전략?
- 로그 보존 정책?

---

### Q93. 커스텀 커널 빌드 전략은?

#### A. 핵심 요약
워크로드에 맞게 커널을 구성하고 최적화합니다.

#### 상세 설명
- menuconfig로 기능 선택
- 모듈 vs 빌트인
- 패치 관리

#### 실무 예시
- 성능 크리티컬 환경
- 하드웨어 특화

#### 면접 답변 예시
**1분**: "필요한 기능만 포함하고, 패치는 버전 관리로 추적합니다."

**3분**: "LTS 커널 기반으로 안정성을 확보하고, 자체 패치는 최소화합니다."

#### 꼬리 질문
- 커널 버전 선택 기준?
- 패치 관리 방법?

---

### Q94. OS 레벨 가상화 기술 비교는?

#### A. 핵심 요약
컨테이너, VM, unikernel 등을 요구사항에 맞게 선택합니다.

#### 상세 설명
- 컨테이너: 빠름, 공유 커널
- VM: 완전 격리, 오버헤드
- Unikernel: 최소화, 특수 용도

#### 실무 예시
- 멀티테넌트 환경
- 엣지 컴퓨팅

#### 면접 답변 예시
**1분**: "격리 요구에 따라 컨테이너(프로세스), VM(OS), unikernel(함수)을 선택합니다."

**3분**: "Firecracker는 마이크로VM으로 컨테이너급 속도와 VM급 격리를 제공합니다."

#### 꼬리 질문
- Firecracker란?
- unikernel 장단점?

---

### Q95. 운영체제 업그레이드 전략은?

#### A. 핵심 요약
롤링, 블루-그린, 카나리 배포로 무중단 업그레이드합니다.

#### 상세 설명
- 라이브 마이그레이션
- 커널 라이브 패치
- 롤백 계획

#### 실무 예시
- 클라우드 인프라 업그레이드
- 보안 패치 적용

#### 면접 답변 예시
**1분**: "롤링 업그레이드로 점진적 적용하고, 문제 시 롤백합니다."

**3분**: "상태 저장 서비스는 마이그레이션이 복잡하여 별도 전략이 필요합니다."

#### 꼬리 질문
- 커널 업그레이드 영향?
- 롤백 자동화?

---

### Q96. 대규모 네트워크 스택 최적화는?

#### A. 핵심 요약
TCP 튜닝, 버퍼 조정, 오프로드로 네트워크 성능을 최적화합니다.

#### 상세 설명
- 소켓 버퍼, 백로그 조정
- TSO, GRO, RSS 오프로드
- 커넥션 테이블 튜닝

#### 실무 예시
- 고트래픽 웹 서버
- 로드 밸런서

#### 면접 답변 예시
**1분**: "버퍼 크기, 백로그, 타임아웃을 조정하고 하드웨어 오프로드를 활용합니다."

**3분**: "TIME_WAIT 재사용, SYN 쿠키로 고동시성을 처리합니다."

#### 꼬리 질문
- SYN Flood 대응?
- BBR 혼잡 제어?

---

### Q97. 스토리지 스택 아키텍처는?

#### A. 핵심 요약
블록 → 파일시스템 → VFS 계층으로 스토리지를 추상화합니다.

#### 상세 설명
- 블록 레이어: I/O 스케줄링
- 파일시스템: 데이터 조직
- VFS: 통합 인터페이스

#### 실무 예시
- 스토리지 성능 분석
- 파일시스템 선택

#### 면접 답변 예시
**1분**: "VFS가 추상화하고, 파일시스템이 구조화하고, 블록 레이어가 I/O를 처리합니다."

**3분**: "각 계층의 캐시(페이지 캐시, 블록 버퍼)가 성능에 영향을 미칩니다."

#### 꼬리 질문
- 블록 레이어 구조?
- 새로운 파일시스템 트렌드?

---

### Q98. 미래 OS 기술 동향은?

#### A. 핵심 요약
Rust 커널, unikernel, 서버리스 런타임이 주목받습니다.

#### 상세 설명
- 메모리 안전 언어 (Rust)
- 마이크로커널 부활
- eBPF 확장성

#### 실무 예시
- Rust for Linux
- AWS Firecracker

#### 면접 답변 예시
**1분**: "Rust로 메모리 안전성을, eBPF로 확장성을, 마이크로VM으로 격리를 강화합니다."

**3분**: "클라우드 네이티브 환경에 맞는 경량, 보안, 고성능 OS로 진화합니다."

#### 꼬리 질문
- Rust for Linux 현황?
- 마이크로커널 장점?

---

### Q99. OS 면접 준비 전략은?

#### A. 핵심 요약
개념 이해, 실습 경험, 트러블슈팅 사례가 핵심입니다.

#### 상세 설명
- 핵심 개념 깊이 이해
- 실제 문제 해결 경험
- 최신 기술 동향 파악

#### 실무 예시
- 면접 대비
- 기술 블로그 작성

#### 면접 답변 예시
**1분**: "개념을 실무 경험과 연결하고, 트러블슈팅 사례를 준비합니다."

**3분**: "깊이와 넓이의 균형, 모르는 것은 솔직히 인정하고 학습 의지를 보여줍니다."

#### 꼬리 질문
- 가장 어려웠던 OS 문제?
- 최근 학습한 OS 기술?

---

### Q100. OS 역량 발전 방향은?

#### A. 핵심 요약
커널 소스 분석, 기여, 최신 기술 추적으로 전문성을 높입니다.

#### 상세 설명
- 커널 소스 읽기
- 오픈소스 기여
- 컨퍼런스, 논문

#### 실무 예시
- 커널 버그 수정
- 성능 개선 패치

#### 면접 답변 예시
**1분**: "소스 분석과 실험으로 깊이를, 커뮤니티 참여로 최신성을 유지합니다."

**3분**: "실제 문제 해결 경험이 가장 좋은 학습입니다. 블로그로 정리하면 이해가 깊어집니다."

#### 꼬리 질문
- 추천 학습 자료?
- 오픈소스 기여 경험?
